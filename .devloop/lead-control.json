{
  "pause": {
    "trace": "until_next_bolt_commit"
  },
  "force_next": "bolt",
  "interval_override": {
    "trace": 6
  },
  "session_manager": {},
  "last_bolt_commit_cycle": 2449,
  "updated_at_cycle": 2446,
  "notes": "C2446 Lead: Directive for Bolt — 3 tasks in priority order: (1) BUG FIX: product-card.tsx line 111 uses undefined `isNew` variable — replace with `const timeElapsed = createdAt ? formatTimeElapsed(createdAt) : null` and render `{timeElapsed && <span className='rounded-md bg-primary px-2 py-0.5 text-xs font-semibold text-primary-foreground'>{timeElapsed}</span>}`. The formatTimeElapsed() function ALREADY EXISTS at top of file — just wire it in. (2) DevChat widget: build src/components/dev-chat/dev-chat-widget.tsx (\"use client\"), import in src/app/(admin)/admin/layout.tsx. Backend 100% ready: DevChatMessage schema + POST/GET/PATCH routes. Full spec in TODO.md [DevChat widget] task. (3) Find similar section on sold product pages: src/app/(shop)/products/[slug]/page.tsx — add sold branch querying similar items. Spec in TODO.md. Trace resumes after Bolt commits.",
  "schema": {
    "pause": "Map of agent_name → condition ('paused' | 'until_next_bolt_commit')",
    "force_next": "Agent name to force as next scheduled, or null. Cleared after use.",
    "interval_override": "Map of agent_name → run_every integer (overrides DB value)",
    "session_manager": {
      "kill_zombie_tasks": "bool — kill tasks stuck as 'running' for >30min",
      "deploy_now": "bool — trigger vercel deploy --prod immediately",
      "requeue_failed": "list of task IDs to requeue (max 5)",
      "reset_no_change_counter": "bool — prevent auto-pause from consecutive no-change cycles"
    }
  },
  "session_manager_last_processed": "2026-04-09T13:55:09.077925"
}