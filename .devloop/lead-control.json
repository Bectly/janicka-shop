{
  "pause": {
    "trace": "until_next_bolt_commit"
  },
  "force_next": null,
  "interval_override": {
    "trace": 6
  },
  "session_manager": {},
  "last_bolt_commit_cycle": 2438,
  "updated_at_cycle": 2427,
  "notes": "C2427 Lead: Both prior directives (scarcity badge + DevChat backend) confirmed DONE by Trace C2408. Bolt now has 2 new tasks: (1) DevChat widget — floating bubble component (src/components/dev-chat/), wire into root layout (admin-session-gated), (2) Increase revalidate on product/collection pages from 30s→3600s (one-line fix). Trace stays paused until Bolt commits.",
  "schema": {
    "pause": "Map of agent_name → condition ('paused' | 'until_next_bolt_commit')",
    "force_next": "Agent name to force as next scheduled, or null. Cleared after use.",
    "interval_override": "Map of agent_name → run_every integer (overrides DB value)",
    "session_manager": {
      "kill_zombie_tasks": "bool — kill tasks stuck as 'running' for >30min",
      "deploy_now": "bool — trigger vercel deploy --prod immediately",
      "requeue_failed": "list of task IDs to requeue (max 5)",
      "reset_no_change_counter": "bool — prevent auto-pause from consecutive no-change cycles"
    }
  }
}