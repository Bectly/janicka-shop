{
  "pause": {
    "trace": "until_next_bolt_commit",
    "bolt": "until_lead_directive"
  },
  "force_next": "bolt",
  "interval_override": {},
  "session_manager": {},
  "last_bolt_commit_cycle": 2554,
  "updated_at_cycle": 2548,
  "dev_focus": "BOLT: Finish delivery deadline customer-facing display — 2 small steps from TODO.md [BOLT] item. (1) In src/app/(shop)/order/[orderNumber]/page.tsx: query expectedDeliveryDate in order fetch, add row below shipping section with calendar icon + 'Předpokládané doručení: {date}' in Czech format 'd. M. yyyy'. (2) In src/lib/email.ts: add expectedDeliveryDate to OrderEmailData, pass it from checkout actions, add one line to HTML template. Run npm run build when done. When done, set next_suggested: lead.",
  "notes": "C2548 Lead — Trace audit DONE (3 duplicate commits C2546-2548, same fix each time = waste). Delivery deadline backend ✅ verified + cron bug fixed. Now Bolt finishes the 2 missing customer-facing steps. Trace paused until Bolt commits.",
  "schema": {
    "pause": "Map of agent_name → condition ('paused' | 'until_next_bolt_commit')",
    "force_next": "Agent name to force as next scheduled, or null. Cleared after use.",
    "interval_override": "Map of agent_name → run_every integer (overrides DB value)",
    "session_manager": {
      "kill_zombie_tasks": "bool — kill tasks stuck as 'running' for >30min",
      "deploy_now": "bool — trigger vercel deploy --prod immediately",
      "requeue_failed": "list of task IDs to requeue (max 5)",
      "reset_no_change_counter": "bool — prevent auto-pause from consecutive no-change cycles"
    }
  },
  "session_manager_last_processed": "2026-04-09T15:21:45.863836"
}