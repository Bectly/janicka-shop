{
  "last_bolt_commit_cycle": 4387,
  "cycle": 4388,
  "directive": "C4388 Lead: #376 DONE (2ec2def — catalog payload trim shipped: images→2, description→160ch, build green) + Sage C4387 scale-95 polish 21 elements shipped (a70ebdd). Bolt force_next pivots to #373 Sage failure triage CLOSE-OUT with concrete fix (currently only diagnostic in_progress — needs tail/categorize/patch cycle) → #374 Czech legacy redirect audit close-out → #377 PDP image quality (q=85-90 main/hero, thumbs q=75) → #341/#343 Hetzner behind #329 DNS. Trace #358 iframe E2E + #367 codebase sweep still force_next. Guard #366 SEC-1 iframe audit still force_next. External blockers unchanged: #329 DNS, #1 R2, #14 GMC (Apr 30 Doppl VTO — 12 days), #216 GA4, #330 nginx, #65 SITE_URL env var (non-blocking).",
  "lanes": {
    "BOLT": {
      "status": "active",
      "force_next_task": 373,
      "next_priority": [
        373,
        374,
        377,
        341,
        343,
        65
      ],
      "notes": "PIVOT: #376 shipped (2ec2def). NOW #373 Sage failure triage CLOSE-OUT — currently in_progress but only diagnostic. Sage logged 610 failures/24h (last: 2026-04-18 09:06:10) vs 62 OK. Concrete steps: (1) tail -n 500 /home/bectly/.claude/jarvis-gym/worker-logs/sage-janicka-shop*.log OR .devloop/worker-logs/sage-*.log — whichever exists; (2) categorize top 3 error signatures with counts (grep+sort+uniq -c); (3) diagnose — likely candidates: playwright chromium launch EBUSY/ENOENT, screenshot timeout, OOM on parallel page loads, missing DEV_URL env; (4) implement concrete fix — add retry with backoff OR serialize screenshot jobs OR fix worker spawn config in .devloop/agents/sage*.{sh,py} OR adjust resource limits; (5) commit with 'Cycle #4388: Bolt — Task #373 close-out' + concrete before/after failure rate rationale. THEN #374 Czech redirect audit close-out (in_progress) — audit src/lib/redirects.ts + proxy.ts/middleware.ts for legacy slug coverage, grep DB for orphan slugs users may hit, extend redirect map, commit. THEN #377 PDP image quality (in_progress) — src/components/shop/product-gallery.tsx or product-detail.tsx: main + hero next/image quality={90} (default 75), thumbnails keep quality={75}. Single commit. Hetzner work (#341/#343/#65) parked behind #329 DNS + env vars.",
      "blockers_external": {
        "329": "DNS flip by bectly",
        "334": "Hetzner daemon must boot",
        "65": "NEXT_PUBLIC_SITE_URL env var needed for Bolt acknowledged #2220 — non-blocking for #376/#373/#377"
      }
    },
    "TRACE": {
      "status": "active",
      "force_next_task": 358,
      "next_priority": [
        358,
        367,
        324,
        327,
        351,
        347
      ],
      "notes": "PRIORITY: #358 RC.7 E2E /admin/jarvis — iframe architecture (not WS/xterm). Playwright spec: admin logs in → navigates /admin/jarvis → expects breadcrumb 'JARVIS' + login instructions card visible → expects iframe present with src matching NEXT_PUBLIC_JARVIS_REMOTE_URL (default jarvis-janicka.jvsatnik.cz) → no console errors (page.on('pageerror') / page.on('console')) → no CSP violations. Skip WS/xterm flow entirely. THEN #367 full codebase quality sweep: npx tsc --noEmit, knip or ts-prune dead exports, security smells (process.env leaks to client, console.log of PII, missing rate limits on POST routes). THEN #324 regression grep: zero 'vinted' campaign refs in HEAD (src/, prisma/, scripts/ — docs/ excepted). THEN #327 Redis cache-aside verify. THEN #351 cold-read audit + #347 Lighthouse baseline. Standing e2e: category switching (/products?category=saty → Topy) + customer portal C3709 flows. DO NOT touch Hetzner migration code paths."
    },
    "SAGE": {
      "status": "active",
      "next_priority": [
        "pdp_polish",
        "cart_sold_item_toast",
        "checkout_accordion_polish",
        "admin_dashboard_polish"
      ],
      "notes": "C4386 shipped active:scale-95 standardization on 24 elements (build green). NEXT micro-passes: (a) PDP — price history sparkline placement (if priceHistory rows exist), condition badge visual weight near title, related-products carousel arrow hover states, image-gallery thumbnail focus ring. (b) Cart sold-item toast when reservation expires (src/components/shop/cart/ — find reservation timer hook). (c) Checkout accordion micro-polish: section transition timings, completed-state checkmark color + animation entrance. (d) Admin dashboard density + order-status badge contrast. UI-only, no migration paths, no schema changes. Note: Bolt #373 is closing out your 610-failure triage — if Sage failures persist across cycles, hold any large passes until worker logs stabilize."
    },
    "GUARD": {
      "status": "active",
      "force_next_task": 366,
      "next_priority": [
        366,
        330
      ],
      "notes": "PRIORITY: #366 SEC-1 Remote Console full-stack audit — iframe architecture (jarvis-janicka.jvsatnik.cz, NOT WS bridge). Scope: (1) frame-src CSP correctness — only jarvis-janicka.jvsatnik.cz, no wildcards, verify in middleware/next.config CSP source; (2) frame-ancestors on /admin/jarvis parent so the page can't be re-framed; (3) iframe sandbox attrs minimum-privilege; (4) CF Access JWT propagation to embedded Claude UI — admin must re-auth at tunnel boundary, no implicit trust; (5) referrer-policy doesn't leak /admin/jarvis pathname to iframe origin; (6) cookies SameSite on parent NextAuth session; (7) audit log integrity for /api/admin/jarvis if still active — userId/userEmail not null, no PII in stderr; (8) confirm jarvis-terminal.tsx + xterm deps fully purged. Deliverable: docs/security/jarvis-bridge-audit.md with findings + severity + fix proposals. THEN #330 nginx hardening — CSP frame-src comgate.eu, rate limits, 20M cap scoped to /api/uploadthing*, X-Forwarded-* sanitization, TLS 1.2+."
    }
  },
  "hard_rules": [
    "DO NOT re-add Vinted campaign (see NEVER_ADD_VINTED_CAMPAIGN.md).",
    "NEVER push to remote without bectly's explicit request.",
    "Sage, Trace, Guard MUST NOT wait on Hetzner DNS/certbot (#329) or daemon boot (#334) — they have independent lanes.",
    "Bolt owns all Hetzner migration code paths (nginx/pm2/env/redis/bullmq/systemd); other agents do not touch them.",
    "Tasks #352, #353, #364, #365, #369, #370, #344, #371, #375 are DONE — do not re-raise as blockers.",
    "Task #375 was rejected (C4386 f8cf472) — do not reopen without fresh production repro."
  ],
  "last_updated": "2026-04-18T09:10",
  "pause": {}
}