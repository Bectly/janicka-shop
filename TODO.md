# Janička Shop — TODO

## ✅ RESOLVED: Vercel/Turso Runtime 500 Error

**Status**: FIXED — all pages return 200 on Vercel production.

**Root causes (multiple)**:
1. Proxy pattern in `db.ts` didn't work with RSC async — removed, migrated all 34 files to `const db = await getDb()`
2. `@prisma/adapter-libsql@7.6.0` was incompatible with `@prisma/client@6.19.3` — downgraded adapter to 6.19.3
3. Prisma 6 adapter API: `new PrismaLibSQL({url, authToken})` (NOT `new PrismaLibSQL(libsqlClient)` which is v7 API)
4. Vercel env vars had trailing `\n` — fixed by re-setting without newlines
5. `DATABASE_URL` must be a valid Prisma URL (`file:./dev.db`), actual Turso URL goes in `TURSO_DATABASE_URL`
6. `getVisitorId()` called `cookies().set()` in Server Components — not allowed in Next.js 16, split into read-only `getVisitorId()` + `getOrCreateVisitorId()` for Server Actions

**Env var setup on Vercel**:
- `DATABASE_URL` = `file:./dev.db` (for Prisma query engine)
- `TURSO_DATABASE_URL` = `libsql://janicka-shop-bectly.aws-eu-west-1.turso.io` (for adapter)
- `TURSO_AUTH_TOKEN` = JWT token (no trailing newline!)

**Fix applied**:
- [x] [BOLT] Removed Proxy, migrated 34 files to `await getDb()` pattern
- [x] [BOLT] Downgraded `@prisma/adapter-libsql` to 6.19.3 (matching @prisma/client)
- [x] [BOLT] Fixed adapter API: `PrismaLibSQL({url, authToken})` direct config
- [x] [BOLT] Fixed Vercel env vars (no trailing newlines, separate TURSO_DATABASE_URL)
- [x] [BOLT] Fixed `getVisitorId()` — read-only in RSC, cookie-setting in Server Actions
- [x] [BOLT] Deployed to Vercel production — all pages return 200

## Phase 1: Foundation [DONE]
- [x] [BOLT] Fix html lang="cs", use Inter font, all metadata in Czech
- [x] [BOLT] Install core dependencies: prisma, @prisma/client, next-auth@5, zustand, zod, react-hook-form, @hookform/resolvers
- [x] [BOLT] Install shadcn/ui CLI and init with theme (pink/rose palette, feminine)
- [x] [BOLT] Create Prisma schema: Product (+ condition, brand, originalPrice, sizes, unique qty=1), Category, Order, OrderItem, Customer, Admin, CartItem, Invoice
- [x] [BOLT] Set up NextAuth v5 with credentials provider for admin
- [x] [BOLT] Create Prisma seed script with sample products (šaty, topy, kalhoty, bundy, doplňky)
- [x] [BOLT] Create base layout: header (logo, nav, cart icon, search), footer, mobile menu

## Phase 2: Product Catalog [IN PROGRESS]
- [x] [BOLT] Homepage: hero banner, featured products, categories grid, newsletter signup
- [x] [BOLT] Product listing page with category filters and sorting (newest, price)
- [x] [BOLT] Product detail page: image gallery, size, condition badge, brand, original vs SH price (discount %), add to cart, similar items
- [x] [BOLT] Search page with results
- [x] [BOLT] "Nově přidané" section on homepage with "Čerstvé kousky za poslední týden" + isNew badge + mobile "Zobrazit všechny novinky" CTA — done (in page.tsx)
- [x] [BOLT] Brand filter — pill-style toggle buttons — done (in product-filters.tsx)
- [x] [BOLT] Size + condition + price range filters on product listing — done (all wired to URL params, server-side filtering)
- [x] [BOLT] Quick view modal on product cards — DONE Cycle #1482 (eye icon on cards → Dialog with product details, server action data fetch)
- [x] [BOLT] Mobile filter drawer — DONE Cycle #2305. Replaced 85vh Sheet with full-screen Drawer (shadcn/vaul, 100dvh + iOS safe-area padding). Accordion sections in research-backed order: Size → Price → Color → Brand → Condition → Sale. Active filter count badges per section. Sticky "Zobrazit X produktů" footer. Sticky floating "Filtry" button at bottom of viewport (thumb zone). Swipe-down to close. Desktop keeps inline grid. **FULL-SCREEN overlay on mobile** (not bottom sheet — Baymard research: too cramped for 5-6 filter facets). Trigger: sticky "Filtry" button pinned to BOTTOM of viewport (thumb zone, 62% of mobile commerce is one-handed), showing active filter count badge. Overlay: slide-up from bottom (300ms ease-out, CSS transform: translateY for 60fps). Inside: vertical accordion sections (one open at a time — prevents scroll fatigue, 32% of sites fail by showing all at once). Sticky footer: "Zobrazit X produktů" button (56px min height, full-width, primary color, count updates in real-time). Close: X button top-right + swipe-down gesture. On desktop (`lg:`): keep current inline grid. Active filter chips stay above product grid after closing overlay. Current Sheet is 85vh — upgrade to 100dvh. **C2286 RESEARCH UPDATE**: ⚠️ Vaul is **explicitly unmaintained** (v1.1.2, last updated Dec 2024, author statement). Use **shadcn Drawer component** instead (wraps Vaul internally, Tailwind v4 compatible, shadcn handles styling conventions). Add `pb-[env(safe-area-inset-bottom)]` to drawer content for iOS safe areas (open PR in Vaul not merged). Responsive pattern: `Dialog` on desktop + `Drawer` (shadcn) on mobile in single component. **Facet order MUST be**: Size → Price → Color → Brand → Condition → Sale (research-backed). Debounce result count updates 200-300ms. Use `React.startTransition` for non-blocking count fetches. Auto-close overlay after "Zobrazit X produktů" tap.
- [x] [BOLT] Filter product counts — DONE. `computeFilterCounts()` in products/page.tsx computes faceted counts per filter group. Counts display as `(N)` next to each option. Zero-count options greyed out but not hidden.
- [x] [BOLT] Color filter — DONE. Color swatches (5px circles with hex colors from COLOR_MAP) + checkmark overlay on selected. Visual filter in product-filters.tsx colorFilterSection.
- [ ] [BOLT] Adopt `nuqs` library for type-safe URL search params — replaces ~50 lines of manual URL parsing in product-filters.tsx. Built-in debouncing (`throttleMs: 500` for price inputs instead of current `onBlur` pattern), server-side cache via `createSearchParamsCache`, batch URL updates. ~10KB. Install: `npm i nuqs`. Add `NuqsAdapter` to root layout. Refactor product-filters.tsx to use `useQueryStates`. Refactor products/page.tsx to use `productFilterCache.parse(searchParams)`.
- [x] [BOLT] Pagination on product listing (12 items/page, reusable Pagination component) — done Cycle #22
- [x] [SAGE] Active filter chips with individual removal — done (FilterChip component with X button + "Smazat vše")
- [ ] [TRACE] E2E test: browse catalog, filter, view product
- [ ] [LEAD] "Poslední kus" scarcity badge on all product cards — honest urgency for qty=1 items (~22% conversion lift per A/B data)
- [ ] [LEAD] "X lidí si prohlíží" real-time viewer counter on product detail — social proof + urgency
- [ ] [LEAD] Brand-aware size guide: show measurements in cm (prsa/pas/délka) per product, plus brand sizing note ("Zara 38 = cca EU 36") — reduces returns significantly for second-hand
- [x] [BOLT] **Product garment measurements field** — DONE Cycle #2296. `measurements` JSON field (chest/waist/hips/length cm) on Product model, 4 number inputs in admin form row, bordered measurements table with Ruler icon on PDP.
- [x] [BOLT] **Fit note field on products** — DONE Cycle #2296. `fitNote` string field (max 120 chars) on Product, text input in admin, italicized on PDP below size selector.
- [x] [BOLT] **Image captions per photo slot in admin** — DONE Cycle #2296. `images` field changed from `string[]` to `{url, alt}[]`, backward-compat parse utility in `src/lib/images.ts`, per-image caption inputs in admin, all 12 image-parsing locations updated across codebase.
- [x] [LEAD] Wishlist with localStorage (no login required) — DONE (wishlist-store.ts, wishlist-button.tsx, /oblibene page, wishlist-header-button.tsx, recently-viewed integration)
- [x] [LEAD] Second image hover on desktop product cards — DONE Cycle #1482 (CSS crossfade on hover, 500ms transition, pure CSS no JS)
- [x] [BOLT] Curated collections/themes — DONE Cycle #2300. Collection model (title, description, slug, image, productIds JSON, featured, sortOrder, active, startDate, endDate), admin CRUD (list/create/edit/delete with product picker), public pages (/collections listing + /collections/[slug] detail with JSON-LD), featured collections section on homepage, sitemap integration, footer link.
- [ ] [LEAD] Apple Pay / Google Pay express buttons at TOP of mobile checkout — research shows placing express payment options ABOVE the form on mobile increases conversion. When Comgate SDK is integrated, Apple Pay + Google Pay buttons should be the first visible element on mobile checkout, before the Kontakt accordion section.
- [x] [BOLT] **Promoted quick filters above product grid** — DONE (in product-filters.tsx lines 398-470). Horizontally scrollable size pills + color swatches above product grid on mobile, with toggle filter on/off. Desktop has full inline filters visible.
- [ ] [LEAD] **Vinted AI-generated photos angle (NEW C2306 — Vinted allows AI photos, buyers can't tell real from fake)** — Scout C2306 confirmed: Vinted explicitly **allows AI-generated product photos** in listings — platform responded to scam reports but did NOT ban AI-generated images. Result: buyers cannot distinguish real photos from AI-generated fakes in listings. This creates a new trust differentiator SEPARATE from the T&C story: Janicka's photos are 100% real, taken by us, of the actual item you'll receive. **Add to messaging mix**: (A) *"Každé foto pořizujeme my — přesně to, co dostaneš."* (B) *"Na Vintedu nevíš, co kupuješ. Fotky mohou být AI. U nás jsou všechny naše, opravdové."* (C) Trust badge copy for homepage: "Reálné foto — žádné AI" with camera icon. **Deploy alongside T&C campaign**: Layer with T&C messaging (April 28–May 1) for compound impact — T&C story + AI photo trust story = double trust advantage. Post in Stories showing our actual photo-taking process (phone on tripod, real garment).
- [ ] [LEAD] **Vinted T&C April 30 messaging window (UPDATED C2306 — EXACT COPY READY, 19 DAYS)** — Vinted T&C effective **April 30, 2026**. **C2298 RESEARCH CONFIRMS**: AI training clause is **non-opt-out, perpetual, worldwide, transferable, sublicenseable** license to use ALL user content for "development and improvement of **artificial intelligence (AI) and machine learning models**." The settings "opt-out" only covers marketing campaigns — AI training CANNOT be opted out. Vinted Trustpilot: **2.1/5, 76% one-star** (unchanged). User quotes: *"Ever since Vinted switched to AI, accounts are being blocked for no clear reason."* **Exact copy ready for April 28–May 1**: (A) *"Tvoje fotky jsou tvoje. Nikdy je nepoužijeme k trénování AI."* — contrasts Vinted's non-opt-out clause directly. (B) *"76 % zákazníků Vintedu dává jednu hvězdu na Trustpilotu. Janičce dávají ❤️"* — verifiable, citeable. (C) *"Vinted si nechá doživotní právo na tvoje fotky. U nás: co nahraješ, to je tvoje."* **Deploy on**: Instagram Stories (April 28) + homepage trust badge update + update messaging section. Czech mainstream media hasn't picked this up yet — window still FULLY OPEN. This is our STRONGEST competitive messaging opportunity of Q2 2026.
- [x] [BOLT] **Instant search: desktop header wiring** (C2289 verified) — `instant-search.tsx` imported and used in `header.tsx` (line 5 + line 46). Desktop done ✅. Mobile-nav still missing — see next item.
- [x] [BOLT] **Instant search: wire into mobile-nav.tsx** — DONE Cycle #2296. InstantSearch component rendered in mobile Sheet nav panel (search trigger at top of navigation drawer).
- [ ] [LEAD] Zalando Pre-Owned competitive response (NEW C1505) — Zalando Pre-Owned is now **live in CZ** (14 markets). 50% of stock sells within 24h. 40%+ of orders mix new and pre-owned. MEDIUM threat — Zalando has massive brand recognition but operates mass-market, not curated. **Action**: (1) Emphasize Janicka's curation advantage in messaging: "Každý kousek ručně vybraný a nafocený" vs Zalando's automated bulk approach. (2) Monitor Zalando Pre-Owned CZ pricing and UX for overlap with our target segment. (3) Consider "Why Janicka?" comparison page highlighting personal curation, guaranteed condition, Czech-local business, fast shipping.

## Phase 3: Cart & Checkout [IN PROGRESS]
- [x] [BOLT] Zustand cart store with persistence (localStorage)
- [x] [BOLT] Cart page: items list, remove, summary
- [x] [BOLT] Checkout page with contact + shipping form + order summary
- [x] [BOLT] Order confirmation page with cart clearing
- [ ] [BOLT] Payment gateway: **Comgate** (Lead C25+C31 research). Server-side: REST API via `fetch` (create/status/refund at `apidoc.comgate.cz`). Client-side: `@comgate/checkout-js` (replaces old `@comgate/checkout` — TypeScript, promise-based, framework-agnostic). Currently supports Apple Pay + Google Pay inline (card number direct entry "being prepared" by Comgate). Architecture: `src/lib/payments/comgate.ts` (REST client), `src/lib/payments/types.ts`, `POST /api/payments/webhook` route. Sandbox first. **Start Plan pricing (C31 verified)**: 0% card fees for 6 months (up to 50K CZK/mo), then Easy plan auto-applies (1% + 0 CZK for standard EU cards, 2% for other EU cards). Bank transfers: 1% + 0 CZK. Monthly fee: FREE on Start. Refund: 5 CZK. Chargeback: 990 CZK. Fees locked until Dec 31, 2026. **Note**: Until Comgate enables direct card entry in SDK, card payments will use redirect flow — Apple Pay + Google Pay are inline via SDK.
- [ ] [BOLT] Stripe payment integration (international fallback)
- [ ] [BOLT] Payment webhook handler: POST /api/payments/webhook (notification_url callback). Verify payment status via GET after receiving notification — never trust webhook payload alone.
- [ ] [BOLT] Accordion single-page checkout UI (Lead C31+C34+C1496+C1505 research: accordion outperforms multi-step by 11-14% in completion rate, ASOS saw 50% abandonment reduction with single-page). **Mobile layout**: Apple Pay / Google Pay express buttons at VERY TOP (above form — **C1496 Stripe data: 2x mobile conversion** when express at top vs bottom, Apple Pay: **+22.3% conversion**). Then accordion: 1) Kontakt, 2) Doprava (Packeta widget), 3) Platba, 4) Shrnutí — each collapses when completed, shows green checkmark. Auto-advance to next section when current is valid (300ms delay for visual feedback). **C1505 CRITICAL**: Collapsed sections MUST show visible inline summaries (Baymard confirmed as established web convention) — e.g., "Jana Nováková, jana@email.cz" for Kontakt, "Zásilkovna — Výdejna Praha 1" for Doprava. Bad: only section header. "Upravit" link to re-expand. Guest checkout ONLY (no registration — **26% abandon at forced signup**, guest checkout = **+45% conversion**). **Trust badges**: max 3 inline with payment form (Lock + "Zabezpečená platba", Comgate logo, "Partner Zásilkovny") — **more than 3-4 types DECREASES conversion by 5-8%** (C1505 finding). Trust badges for unfamiliar brands: **+15-30% conversion**. **Single name field**: "Celé jméno" instead of Jméno + Příjmení — parse server-side. **Phone conditional**: show only for home delivery. **Target 6-7 fields**: email, full name, phone (conditional), street (autocomplete), city (auto-fill), ZIP (auto-fill), payment. **BNPL**: Comgate native pay-in-3 for items above 500 CZK. Use shadcn Accordion component (`npx shadcn add accordion`). react-hook-form `useFormContext` for shared state.
- [x] [LEAD] Show shipping cost estimate in cart summary — DONE Cycle #1489 (free shipping progress bar with animated fill, "Doprava od 69 Kč" hint, green "Doprava zdarma!" when threshold met)
- [x] [LEAD] Server-side cart validation at checkout initiation (NEW C1484) — DONE. `createOrder` action in checkout/actions.ts performs full server-side validation inside a DB transaction: verifies product availability (sold/active/reserved), uses DB prices (never trusts client), validates sizes/colors against DB data, calculates shipping/totals server-side. Rejects unavailable items with clear Czech error messages.
- [ ] [LEAD] PWA manifest + service worker for mobile (NEW C1484) — Fashion PWAs show **70% higher conversion** vs mobile web. Kaporal (French fashion): 60% lower bounce, 15% higher desktop conversion, 40% longer visits. Butcher of Blue (Dutch fashion): **169% conversion increase**, 154% more MAU. Implementation: (1) `manifest.json` with Janicka branding (name, icons, theme_color: rose), (2) `next-pwa` or Serwist for Next.js 15 service worker, (3) cache product images + static assets for offline-capable browsing, (4) contextual install prompt AFTER engagement (3rd visit or add-to-cart — NOT immediate popup, achieves 15-25% install rates). Post-launch priority but high ROI for mobile-dominant audience (62% of CZ e-commerce is mobile).
- [ ] [LEAD] Address autocomplete in checkout form (NEW C1496, UPDATED C2293/Scout8) — Address autocomplete: **ASOS case study**: entry dropped from 62s → 8s, resulting in +12% completed transactions. -35% cart abandonment, -30% mobile checkout time. **C2293/Scout8 RECOMMENDATION: Use Mapy.com API instead of Google Places.** (1) **Mapy.com API** (developer.mapy.com): 250,000 API credits/month FREE — sufficient for entire launch phase. Best CZ address coverage (Seznam maps). "Našeptávání adres" explicitly supported. REST + JSON. Pricing overage: 1.6 CZK per 1,000 credits — effectively free. (2) Google Places API: $200/month free credit, use `components=country:cz`. Better fallback for international addresses. (3) RUIAN (ČÚZK) — free but complex. **Recommendation**: Mapy.com as primary, Google as optional fallback. `@comgate/checkout` is unrelated — this is a pure address field enhancement.
- [ ] [LEAD] Express checkout buttons on cart page (NEW C1496) — Apple Pay / Google Pay buttons should appear not only at TOP of checkout, but ALSO on the **cart page** alongside the primary checkout CTA. **Stripe data across 150k+ sessions**: express checkout at top = **2x mobile conversion**. Apple Pay specifically: **+22.3% conversion, +22.5% revenue**. Express checkout reduces flow from ~120 clicks to **4 clicks**. Multiple placement points (PDP, cart, checkout) maximize conversion capture. Payment diversity lifts conversion **12-15%**. Digital wallets now **49-56% of global e-commerce transaction value**. For cart page: show "Zaplatit přes Apple Pay" + "Zaplatit přes Google Pay" buttons above the standard "Pokračovat k pokladně" button on mobile.
- [x] [LEAD] HTML autocomplete attributes on ALL checkout form fields — DONE. All visible checkout inputs have correct `autoComplete` values: `given-name`, `family-name`, `email`, `tel`, `street-address`, `address-level2`, `postal-code`.
- [x] [LEAD] Phone field optional or conditional in checkout — DONE Cycle #2286. Phone field hidden when `isPacketaPickup === true`, required only for home delivery methods. `createOrder` action updated to handle optional phone. 39% mobile abandonment at phone fields prevented.
- [x] [LEAD] Exit intent popup on cart page — DONE. `CartExitIntent` component in `cart-exit-intent.tsx`: triggers on mouse toward viewport top, shows product thumbnails with urgency messaging ("Tyto kousky jsou unikáty — kdokoliv je může koupit"), once per session (sessionStorage), dismiss on click/backdrop/close.
- [ ] [LEAD] Post-purchase welcome email + progressive profiling (NEW C1496) — Email is the **#1 converting traffic source at 19.3%**. Automated welcome emails achieve **83.63% open rate** and **16.60% CTR** (vs standard campaigns). After guest checkout (email already captured), send welcome email with: (1) order confirmation (already done), (2) **optional account creation** link ("Chceš sledovat objednávky? Nastav si heslo jedním kliknutím"), (3) personalized product recommendations based on purchased category/size. Progressive profiling: ask for preferences (favorite brands, sizes) on 2nd interaction — **120% higher conversion** with ≤5 fields vs long forms. Segmented campaigns: **+760% revenue**. This turns one-time buyers into repeat customers without forcing registration upfront.
- [x] [BOLT] **GDPR marketing consent checkbox at checkout** — DONE Cycle #2300. `marketingConsent Boolean @default(false)` on AbandonedCart model. Opt-in checkbox in checkout Kontakt section: "Upozornit mě, pokud zboží v košíku nákoupí někdo jiný." Consent passed to `captureAbandonedCart()` and stored in DB. Cron job `/api/cron/abandoned-carts` now filters all 3 email queries by `marketingConsent: true` — no emails sent without explicit opt-in.
- [ ] [TRACE] E2E test: full checkout flow
- [x] [BOLT] Cart reservation timer (15 min) — DONE Cycle #27. Atomic TOCTOU-safe reserve/release/extend server actions, countdown timer in cart, "Rezervováno" badges on product cards/detail, reservation-aware checkout. TOCTOU race fixed Cycle #29.

## Phase 4: Admin Panel [IN PROGRESS]
- [x] [BOLT] Admin layout with sidebar navigation
- [x] [BOLT] Dashboard: today's orders, revenue, product stats
- [x] [BOLT] Products CRUD: list, create, edit, delete
- [x] [BOLT] Orders management: list with status filters, detail with status update (color badges)
- [x] [BOLT] Categories management: list, create, edit, delete
- [x] [BOLT] **IMAGE UPLOAD** — UploadThing v7 integration. File router with auth middleware (max 10 files, 4MB each), admin ImageUpload component with drag-to-reorder + delete + preview, ProductCard renders real images via next/image, ProductGallery on detail page with thumbnails + arrow nav. NextSSRPlugin in root layout, Tailwind v4 CSS import, next.config.ts remotePatterns for ufs.sh. Mobile upload works via native camera picker.
- [x] [BOLT] Customers list with order history — DONE (admin/customers list page + detail page with stats, order history, contact info)
- [x] [BOLT] Settings: shop info, payment config, shipping config — DONE (admin/settings page with ShopSettings model, form, server actions)
- [x] [BOLT] **Product duplicate/copy** — DONE Cycle #2303. `duplicateProduct()` server action copies all product data (name + " (kopie)", auto slug + SKU, clears sold/reserved, starts hidden). Copy icon button on products list. Redirects to edit page.
- [ ] [TRACE] Admin CRUD tests

## Phase 5: devChat — Owner ↔ Lead Communication
- [ ] [BOLT] Prisma model: DevChatMessage (id, message, page_path, page_title, sender, status, priority, response, created_at, resolved_at)
- [ ] [BOLT] API routes: POST /api/dev-chat (send message + page context), GET /api/dev-chat (list, filter by status/page), PATCH /api/dev-chat/[id] (resolve + respond)
- [ ] [BOLT] DevChatWidget component: floating bubble (bottom-right), expand to chat panel, auto-detect current page path + title
- [ ] [BOLT] Chat UI: message list (owner vs lead messages), input field, page context badge, status indicators (new/read/resolved)
- [ ] [BOLT] Unread badge on chat bubble (count of unresolved messages)
- [ ] [BOLT] Auth guard: only logged-in admin (Janička) can use devChat
- [ ] [BOLT] Lead integration: GET /api/dev-chat?status=new returns new messages for Lead agent to process
- [ ] [TRACE] E2E test: send message from page, verify page context captured, verify Lead can read it

## Phase 5b: Pick Pages — Lead → Janička rozhodovací stránky
- [ ] [BOLT] Prisma model: DevPick (id, slug, title, description, pick_type, options JSON, selected_option, custom_text, status, default_option, created_at, answered_at, expires_at)
- [ ] [BOLT] API routes: POST /api/dev-picks (create pick), GET /api/dev-picks/[slug] (pick data), PATCH /api/dev-picks/[slug] (answer), GET /api/dev-picks?status=answered (Lead reads)
- [ ] [BOLT] Pick page /pick/[slug]: krásný klikací UI, velké karty s preview, komentář ke každé volbě, mobile-first
- [ ] [BOLT] Pick types: image_choice (obrázky), choice (text options), text (volný input), rating (1-5)
- [ ] [BOLT] Auto-expiry: pokud Janička neodpoví do expires_at, Lead vybere default_option
- [ ] [BOLT] Notifikace: při vytvoření picku → devChat zpráva + volitelně Resend email
- [ ] [BOLT] Redis pub při odpovědi → Lead se probudí okamžitě
- [ ] [TRACE] E2E test: create pick, answer it, verify Lead can read answer

## Phase 6: Shipping & Invoicing
- [ ] [BOLT] Packeta/Zásilkovna pickup point widget v6 (UPDATED C31) — load `https://widget.packeta.com/v6/www/js/library.js` via `next/script` in "use client" component. Call `Packeta.Widget.pick(apiKey, callback, { language: "cs", view: "modal", vendors: [{ country: "cz" }] })`. Callback receives `point` with `name`, `id`, `street`, `city`, `zip`. Store selected point data in checkout form state. Requires HTTPS. Widget configurator: `configurator.widget.packeta.com`. Validation endpoint: `widget.packeta.com/v6/pps/api/widget/v1/validate`. API docs: `docs.packeta.com`.
- [ ] [BOLT] Packeta shipment creation: SOAP API (createPacket) or REST at docs.packeta.com. Auth via apiPassword. Generate A4 labels.
- [ ] [BOLT] PDF invoice generation with mandatory CZ fields: IČO, DIČ, seller address, buyer info, invoice number, dates, itemization, VAT status ("Nejsem plátce DPH" if applicable)
- [ ] [BOLT] Email notifications via Resend (`resend` npm + `@react-email/components`): order confirmation, payment received, shipping dispatched, invoice PDF attachment. Use Server Actions with "use server" — never expose RESEND_API_KEY client-side. Build React Email templates for consistent branding.

## Phase 7: Legal Compliance [UPDATED — LEAD RESEARCH C19, C37]
- [x] [BOLT] Cookie consent banner — DONE Cycle #27. Granular categories (essential/analytics/marketing), same-size Accept/Reject buttons, no dark patterns, localStorage persistence, conditional script loading. Re-consent mechanism + footer button added Cycle #29. Secure flag on HTTPS fixed Cycle #30.
- [x] [BOLT] Withdrawal form (vzorový formulář pro odstoupení od smlouvy) — DONE. Full form at `/returns/withdrawal-form` with print support, legal info (§1829, §2167), addressee section, product/order fields, consumer details, signature, bank account for refund. Print-optimized CSS. Instructions for use. Returns parent page at `/returns` with link.
- [x] [BOLT] Footer legal links: ODR platform (ec.europa.eu/odr), ČOI as supervisory authority — DONE (in footer.tsx)
- [ ] [BOLT] Update obchodní podmínky (terms page): add 12-month warranty clause for used goods (§2167 Civil Code), seller IČO + sídlo, all payment/delivery terms, withdrawal rights
- [ ] [BOLT] Update privacy policy page: GDPR-compliant, separate from T&C, data purposes, retention, rights, legal basis
- [ ] [LEAD] Sustainability claims audit: EU Directive 2024/825 (effective 2026) — greenwashing fines up to 5M CZK. All eco/sustainability claims must be specific and verifiable. "Ušetříš 70 % oproti nové ceně" is OK, vague "ekologické" is NOT.
- [ ] [LEAD] GPSR compliance for second-hand resale (NEW C1493) — EU General Product Safety Regulation in effect since **Dec 13, 2024**. E-commerce product pages MUST display: (1) manufacturer name + contact details, (2) product identification including images, (3) any warnings/safety info. For second-hand resale: original brand is the "manufacturer" — display brand name (already done) + verify if brand contact info is required for resale. **Heureka impact**: GPSR tags (`MANUFACTURER_POSTAL_ADDRESS`, `MANUFACTURER_ELECTRONIC_ADDRESS`) now required in Heureka XML feed — already in TODO but this is the legal basis. **Risk**: non-compliance = product listing removal from marketplaces. Low effort to add manufacturer field to product model if needed. Research: some EU marketplaces (Amazon, eBay) already enforcing GPSR data requirements — Heureka likely to follow.

## Phase 7c: EU Accessibility Act (EAA) Compliance [C37, UPDATED C40] — RECOMMENDED (NOT legally required)
Czech Act No. 424/2023 Coll. — in force since June 28, 2025. **⚠️ C40 UPDATE: MICRO-ENTERPRISE EXEMPTION APPLIES.** Zákon 424/2023 Sb. exempts mikropodniky (<10 employees AND <€2M turnover) from SERVICE requirements. Janicka Shop qualifies. Accessibility is NOT a legal launch blocker but remains a COMPETITIVE ADVANTAGE (none of CZ second-hand competitors are WCAG-compliant) and GOOD UX PRACTICE. Implement incrementally. If Janicka grows past threshold, full compliance becomes mandatory.
- [ ] [LEAD] Accessibility audit of existing pages — run axe-core or Lighthouse accessibility audit on all current pages. Identify violations against WCAG 2.1 AA. Prioritize fixes by severity (critical → serious → moderate).
- [ ] [BOLT] Semantic HTML audit + fix — ensure all pages use correct heading hierarchy (h1→h2→h3, no skips), landmark elements (`<nav>`, `<main>`, `<header>`, `<footer>`, `<aside>`), lists for navigation items, `<button>` for actions (not div onclick). Check all existing components.
- [ ] [BOLT] Keyboard navigation for product filters — ALL filter controls (brand pills, size buttons, condition, price range, color swatches, sort dropdown) must be operable via keyboard. Tab order must be logical. Active filter chips must be keyboard-removable. Focus must return to logical position after filter change.
- [ ] [BOLT] Keyboard navigation for checkout — accordion sections must be keyboard-operable (Enter/Space to expand/collapse). All form fields accessible. Packeta widget keyboard support verification. Payment buttons reachable via Tab.
- [ ] [BOLT] Visible focus indicators — add `focus-visible` outlines to ALL interactive elements (buttons, links, inputs, selects, custom controls). Use consistent, high-contrast focus ring (e.g. 2px solid with offset). Tailwind: `focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-pink-500`. NEVER use `outline-none` without `focus-visible` replacement.
- [ ] [BOLT] Alt text on all product images — every `<img>` and `next/image` must have descriptive alt text. Product images: "[Brand] [Product name] - [condition]" (e.g. "Zara letní šaty - výborný stav"). Decorative images: `alt=""`. Category icons: descriptive alt. Hero/banner images: descriptive alt.
- [ ] [BOLT] Color contrast compliance — verify ALL text meets WCAG 2.1 AA contrast ratios: ≥4.5:1 for normal text (<18px or <14px bold), ≥3:1 for large text (≥18px or ≥14px bold). Check: condition badges, price text, discount percentages, muted/secondary text, placeholder text, filter labels. Use Chrome DevTools or axe-core. Fix any violations.
- [ ] [BOLT] Accessible forms — all form inputs must have visible `<label>` elements (not just placeholder text). Error messages must be associated via `aria-describedby`. Required fields marked with `aria-required="true"`. Form validation errors announced to screen readers (aria-live region or role="alert").
- [ ] [BOLT] Screen reader support — add `aria-label` to icon-only buttons (cart icon, search icon, close buttons, heart/wishlist). Add `aria-live="polite"` regions for dynamic content updates (cart count, filter results count, reservation timer). Add `sr-only` text for visual-only information (discount percentage, condition dots).
- [ ] [BOLT] Focus management on route changes — Next.js App Router doesn't auto-manage focus on navigation. After route change, focus must move to `<main>` or page heading (use `tabindex="-1"` + programmatic `.focus()`). Announce page changes to screen readers via aria-live region. Critical for product listing → product detail → back navigation.
- [ ] [BOLT] Modal/drawer accessibility — all modals (quick view, mobile filters, cart drawer, cookie consent) must: trap focus inside when open, return focus to trigger element on close, close on Escape key, have `role="dialog"` + `aria-modal="true"` + `aria-labelledby`. Shadcn Dialog/Sheet components handle most of this — verify.
- [ ] [BOLT] Skip navigation link — add "Přeskočit na obsah" (skip to content) link as first focusable element on every page. Visible on focus only (sr-only + focus-visible:not-sr-only). Links to `#main-content` on `<main>`. Standard accessibility pattern.
- [ ] [TRACE] WCAG 2.1 AA compliance test — automated (axe-core/Lighthouse) + manual keyboard-only navigation test of all critical flows: browse catalog → filter → view product → add to cart → checkout. Every flow must be completable without a mouse.

## Phase 7b: Onboarding Page — Dárek od JARVIS
- [ ] [BOLT] Install canvas-confetti npm package
- [ ] [BOLT] Route /admin/welcome — fullscreen welcome page, no admin sidebar/header
- [ ] [BOLT] Middleware: po prvním admin loginu redirect na /admin/welcome (check admin.onboarded_at === null)
- [ ] [BOLT] Věnování od JARVIS — animovaný text s postupným fade-in, kurzíva, osobní tón, srdíčka
- [ ] [BOLT] Agent karty — Lead, Bolt, Trace, Scout, JARVIS — vtipné popisky + fake statistiky
- [ ] [BOLT] Reálné statistiky z DB: počet cyklů, commitů, řádků kódu, Lead direktiv, dní vývoje
- [ ] [BOLT] Konfety efekt (canvas-confetti) při načtení stránky
- [ ] [BOLT] CTA "Jdu si to prohlédnout →" — PATCH /api/admin/onboard (set onboarded_at) + redirect na dashboard
- [ ] [SAGE] Design: rose/pink gradient, bílé karty, parallax, staggered animations, serif font pro věnování
- [ ] [SAGE] Mobile-first — musí být krásná na mobilu (Janička to uvidí poprvé na telefonu)

## Phase 8: Polish, SEO & Conversion [UPDATED — LEAD RESEARCH C19]
- [ ] [SAGE] Mobile-first responsive polish — every page (fashion e-commerce is 70%+ mobile)
- [ ] [SAGE] Animations: page transitions, cart interactions, hover effects (Framer Motion)
- [x] [SAGE] Swipeable product image gallery on mobile — DONE Cycle #1497 (touch swipe gestures with direction-locked handling, swipe-follow feedback, mobile image counter "1/5", lightbox swipe support). Fixed Cycle #1499 (pan-drag zoom toggle bug, stale closure in swipe handler).
- [x] [BOLT] SEO structured data — Product JSON-LD with itemCondition, offers, brand, category, image on product detail pages. XSS-safe (\\u003c escaping). — done Cycle #22, hardened Cycle #24
- [x] [BOLT] SEO basics: Open Graph tags (og:image), sitemap.xml (dynamic from products/categories), robots.txt — done Cycle #22
- [x] [BOLT] Enrich JSON-LD with `shippingDetails` + `hasMerchantReturnPolicy` — DONE Cycle #1491 (all 3 shipping options with free threshold, MerchantReturnPolicy, color/size/condition as PropertyValue attributes, priceValidUntil). Enhanced Cycle #1495 (fixed @type errors).
- [x] [LEAD] 30-day price history tracking for discount compliance — DONE (price-history.ts with PriceHistory model, getLowestPrices30d() utility, integrated across homepage/catalog/search/product detail pages). Czech Omnibus Directive compliant.
- [ ] [BOLT] Performance: image optimization (WebP/AVIF via next/image), lazy loading, ISR for product pages
- [ ] [LEAD] **⚠️ UploadThing billing audit (C2304 Scout — URGENT pre-launch)** — UploadThing FREE tier is GONE. Migrated to **$25/month base, 250GB included, $0.08/GB overage** (no bandwidth charges). Action: (1) Log into UploadThing dashboard, verify current billing plan, (2) Update CLAUDE.md and Vercel env docs if account is still on old free tier (may have been grandfathered or needs upgrading), (3) At $25/month: 250GB covers ~500K product images at 500KB each — sufficient for entire shop lifecycle. (4) Adding video to same plan: zero incremental cost. Budget line: $25/month UploadThing must be accounted for in operational costs. Cloudflare Stream ($2-3/month) is an optional add-on for HLS video quality later.
- [x] [LEAD] Sticky "Přidat do košíku" button on mobile product detail — DONE Cycle #1489 (IntersectionObserver on #atc-sentinel, fixed bottom bar with product name + price + CTA, mirrors all button states)
- [x] [LEAD] Size guide + fit info — DONE Cycle #1485 (size guide modal with EU charts for tops/dresses/jackets and bottoms/skirts, measurement instructions in Czech, linked next to size selector). Sizing uncertainty = #1 conversion killer. Our exposed button selectors are correct ✅ (57% of sites still use dropdowns — Baymard). **Remaining**: per-brand measurement data, model measurements in cm on specific products (post-launch when we have data).
- [ ] [TRACE] Core Web Vitals audit — target INP ≤ 200ms (Google's 2026 "good" threshold for media-heavy pages)
- [ ] [GUARD] Rate limiting: @upstash/ratelimit + Vercel KV (Redis) for production. Apply to: checkout/order creation (5/min), login attempts (5/15min), search (30/min). Edge middleware approach for Vercel compatibility. Currently MEDIUM priority (flagged Cycle #19).
- [ ] [GUARD] Security audit: CSRF, input sanitization, session security (security headers already done in Cycle #19)
- [ ] [LEAD] Product image descriptive overlays/annotations (NEW C1487) — Baymard 2026: **52% of e-commerce sites** lack descriptive text or graphics for product images. For second-hand fashion, this is CRITICAL: annotate product photos with text labels showing material, condition details ("drobné opotřebení na lemu"), or fit notes ("volnější střih"). Can be done as caption text below gallery images or as semi-transparent overlay badges. Helps customers assess quality without physical inspection — the #1 barrier in online second-hand. Low effort, high trust-building impact.
- [ ] [BOLT] Next.js 16 Cache Components adoption (UPDATED C1493, CRITICAL NOTE C2293) — We're on Next.js 16.2.2. **C2293 CRITICAL FINDING**: TTFB drops **60-80%** with `"use cache"` on product pages; static shells serve in sub-100ms. **⚠️ ISR CONFLICT**: enabling `cacheComponents: true` in `next.config.ts` automatically disables traditional ISR cache — they cannot run simultaneously. Migration: (1) Remove `force-dynamic` from product/catalog pages, (2) add `"use cache"` + `cacheLife("hours")` to static product data fetches, (3) keep reservation/price/cart dynamic via `connection()`. Start with product detail page (highest TTFB impact), measure before/after on Vercel Analytics. With 50+ products on catalog page, shared layout downloads once instead of 50x. **Turbopack** already active.
- [ ] [BOLT] Migrate middleware.ts → proxy.ts (NEW C1493) — Next.js 16 deprecated `middleware.ts` filename in favor of `proxy.ts`. Our middleware is simple (NextAuth admin route protection). Migration: rename file + function name. Codemod available: `npx @next/codemod@latest middleware-to-proxy`. **Note**: proxy runtime is `nodejs` (NOT edge). If NextAuth auth function requires edge, keep as middleware for now — but verify. Low effort, prevents future breakage when middleware support is removed.
- [ ] [BOLT] **⚠️ SECURITY: Next.js 16.2.x CVE patch** (NEW C1511 — **URGENT**) — Two security vulnerabilities in current 16.2.2: (1) **CVE-2026-27979**: `maxPostponedStateSize` enforcement bypass, (2) **CVE-2026-29057**: http-proxy request smuggling in rewrites. Fix: `npm update next` to latest 16.2.x patch. Also includes: Image LRU disk cache, streaming fetch hang fixes, Turbopack stability improvements. **Do this BEFORE any other work.**
- [ ] [BOLT] shadcn/ui Radix migration (NEW C1511 — **QUICK WIN**) — shadcn/ui shipped unified `radix-ui` package (Feb 2026). Single import replaces ALL separate `@radix-ui/react-*` packages (~15 dependencies). Migration: `npx shadcn@latest migrate radix "src/components/ui/**"`. Reduces dependency count, simplifies package.json, no behavioral changes. 5-minute task.
- [ ] [LEAD] WhatsApp Business API for cart recovery (NEW C1511, **PRICING ASSESSED C2304 — POST-LAUNCH ONLY**) — WhatsApp cart recovery achieves **80% open rate** and **5x ROI** (vs email 50% open, 3-5% recovery). Multi-channel (email + WhatsApp) = **2-3x higher engagement**. **⚠️ C2304 PRICING REALITY CHECK**: CZ falls under "Rest of Central & Eastern Europe" — marketing messages = **$0.086/msg** (~2.15 CZK). PLUS mandatory BSP (Business Solution Provider) fee of **$50-500/month**. For a micro-business doing 20-50 cart recoveries/month, BSP fee alone makes this uneconomical at launch. **Decision: Email-first (Resend, already integrated) is correct for launch. Evaluate WhatsApp when monthly revenue exceeds ~50K CZK/month AND email recovery is fully established.** If proceeding: use Manychat or Wati (lowest BSP fees at $15-49/month for small plans). Free WhatsApp Business App (no API) is fine for 1-2 daily manual follow-ups as stopgap. Providers for CZ: Meta Cloud API direct (no BSP fee, but requires developer setup) OR Manychat/Wati for no-code.
- [ ] [LEAD] "Holy grail hunting" messaging alignment (NEW C1511 — ThredUp 2026 insight) — Gen Z has shifted from secondhand "hauls" to curated **"holy grail" hunting** — finding unique, special pieces. 62% of Gen Z shopped secondhand in 2025. This is EXACTLY Janicka's positioning. Update messaging across: (1) homepage hero/tagline to emphasize uniqueness and curation, (2) product cards — "Unikátní kousek" instead of generic badges, (3) social sharing — lean into the treasure-hunt narrative. "Každý kousek je originál. Žádné masové kolekce." Also: 38% of Gen Z/millennials engage in social resale commerce (3x rate of older gens) — optimize sharing UX for this demographic.

## Phase 8b: Vinted Product Migration [NEW — bectly request 2026-04-09]
Migrace VŠECH produktů a fotek z Janičky Vinted profilu (https://www.vinted.cz/member/149371637) do Janička Shop.
Skripty připraveny v `scripts/` — Bolt implementuje zbývající části.

- [ ] [BOLT] **Test & fix Vinted scraper** (`scripts/scrape-vinted.ts`) — run `npx tsx scripts/scrape-vinted.ts`, verify it collects all product URLs from the profile (infinite scroll), navigates to each detail page, extracts: title, description, price, brand, size, condition, color, category, material, ALL photo URLs. Fix any selectors that don't match current Vinted DOM (Vinted changes their markup frequently — inspect live page, update `data-testid` selectors and fallbacks). Download ALL photos to `scripts/vinted-data/images/{vintedId}/`. Save progress to `scripts/vinted-data/products.json` after each product (crash recovery). Handle cookie consent banner. Rate limit: 2s between pages minimum. Take debug screenshot if 0 products found. **AUTH**: Playwright needs Vinted session — try Flatpak Chrome profile at `~/.var/app/com.google.Chrome/config/google-chrome/` first. If that fails, try extracting cookies manually or using `playwright codegen` for manual login flow. Target: 100% of listed products scraped with ALL photos.
- [ ] [BOLT] **Vinted API intercept approach** (ALTERNATIVE if DOM scraping is flaky) — Vinted frontend calls `GET /api/v2/users/{id}/items?page=N&per_page=96` and `GET /api/v2/items/{id}` for detail. Use Playwright `page.route()` to intercept these API responses — they contain ALL structured data (title, description, price, brand, size, condition, colors, photos array with all sizes, measurements, category tree, material). This is MORE RELIABLE than DOM scraping. Flow: (1) navigate to profile, (2) intercept XHR responses matching `/api/v2/`, (3) extract JSON data directly, (4) download photo URLs from the `photos[].full_size_url` field. Fall back to DOM scraping only for fields not in API response.
- [ ] [BOLT] **Implement DB import** in `scripts/import-vinted-products.ts` — the script has mapping logic ready (condition, category, slug, SKU generation). Bolt needs to: (1) upload local photos to UploadThing (batch, respect rate limits), (2) look up Category IDs from DB by slug, (3) create Product records via Prisma with all mapped fields, (4) create PriceHistory entries for 30-day price compliance, (5) handle duplicates (skip if vintedId already imported — store vintedId in product metadata JSON), (6) set products as hidden/draft by default (Janička reviews before publishing). Run with `--dry-run` first to verify mapping. Support `--publish` flag for auto-activate.
- [ ] [BOLT] **Admin: Vinted import review page** — `/admin/imports/vinted` page showing all imported products in a grid. Each card shows: Vinted photo, mapped title, price, brand, size, condition, mapped category. Status badges: "Unmapped category" (yellow), "No photos" (red), "Ready" (green). Bulk actions: publish selected, edit category, delete. Filter by status. This lets Janička quickly review and publish imported products.
- [ ] [TRACE] **Verify import data quality** — after import, audit: (1) no missing titles or prices, (2) all photos downloaded and accessible, (3) condition mapping is correct (spot-check 10 random products), (4) category mapping covers >90% of products (flag unmapped), (5) no duplicate slugs, (6) SKUs are unique, (7) price history entries created. Run `npm run build` to verify no type errors from new data.

## Phase 9: Growth & Engagement [UPDATED — LEAD RESEARCH C19, C40]
- [ ] [LEAD] Saved search alerts — let users save filters (size + category + price range) and get email when matching new items arrive. Vinted's #1 most-requested missing feature. Major differentiator.
- [ ] [LEAD] "Nově přidané" drop strategy — batch new items, announce at consistent daily time (e.g., 18:00) to train repeat visits. Push/email notification to subscribers.
- [x] [LEAD] "Právě prodáno" live feed on homepage — DONE Cycle #1496 (8 most recently sold items, greyed-out images, "Prodáno" badges, relative timestamps, horizontal scroll on mobile)
- [ ] [LEAD] Wishlist with notifications — notify when a favorited item's price drops or similar items arrive
- [ ] [BOLT] **Wishlist sold-item email notification (NEW C2298/Scout9 — 40% conversion rate)** — When a wishlisted item sells to another customer, fire email within 30 minutes: "Bohužel, [Produkt] už má novou majitelku. Ale vybrali jsme ti [3 alternativy z kategorie+velikosti]." **Once Again (same-model second-hand shop) case study: 40% conversion rate on these emails** — dramatically outperforms cart abandonment (10-14%). Why: customer expressed specific intent, someone else took it (validates scarcity), alternatives immediately offered. Architecture: on `Order` creation, loop through wishlisted-product IDs, find users with that product in wishlist, fire Resend email. Requires `WishlistItem` model (currently localStorage-only — needs server-side storage or email capture). Guest wishlist: localStorage is fine; sold notification is only possible for registered/email-captured users.
- [ ] [BOLT] **New arrival email within 2h of publishing (NEW C2298/Scout9 — 72% convert in first 24h)** — When admin publishes a new product, fire notification email to newsletter subscribers who have matching saved-search criteria (size + category + price range). **72% of buyers who convert from recovery emails do so in the first 24 hours.** Subject line with brand + size in it drives highest opens. Send within 2 hours of publish, not batched daily. Include "Tento kousek může být pryč do hodiny" — authentic scarcity for qty=1 items. Architecture: on `Product.status` change to `available`, trigger background job → match subscribers → Resend dispatch. **Newsletter subscriber preference fields DONE** (Cycle #2303): `preferredSizes`, `preferredCategories`, `preferredBrands` JSON fields + `firstName`, `source` on NewsletterSubscriber model. Admin page shows preference badges. `updateSubscriberPreferences()` server action for progressive profiling. Start simple: no-preference subscribers get new-arrival digest 2x/week; preference-matched get immediate.
- [ ] [LEAD] Customer reviews / social proof on homepage — even 5 reviews can lift conversions ~270% per industry data. **Review sweet spot (C2304 Shopify CRO 2026 CONFIRMED): 4.0-4.7 rating range is perceived as most credible — 5.0 reads as "too good to be true" or curated.** When building reviews: do NOT hide negative reviews, display honest ratings. Products with 11-30 reviews: +68% conversion. Surface UGC showing different body types. Complement with Heureka "Verified by Customers" for institutional trust signal.
- [ ] [LEAD] Heureka.cz "Verified by Customers" — NEW pricing model (Sept 2025): free "Start" tier (15 reviews/month), paid "Profi" (499 CZK/month, returned as ad credit). 50% of Czech shoppers ONLY buy from Heureka-certified shops. Start with free tier. Integration: XML product feed for Heureka zbožák + review widget. THE #1 Czech trust signal — Brumla has 99% rating.
- [ ] [LEAD] Instagram Shopping product feed + micro-influencer strategy — Instagram is THE social channel for CZ women 18-35 (70% of CZ internet users follow influencers, CZ influencer spend surpassed $95M). Plan: Instagram Shopping catalog feed, UGC (real customers wearing purchased items), 3-5 micro-influencers in CZ fashion/sustainability niche. TikTok Shop NOT available in CZ — focus 100% on Instagram.
- [ ] [LEAD] Messaging strategy: lean into "My jsme to už zkontrolovali, aby ses nemusela" — key differentiator vs Vinted (inconsistent quality, scams, random sellers, recent backlash over grouped sizing). Janicka = curated quality, pro photos, guaranteed condition, single-warehouse fast shipping.
- [x] [BOLT] QR code payment on order confirmation — DONE (C2293 audit). `src/lib/payments/qr-platba.ts` (SPAYD generator via `spayd` npm + `qrcode` npm), `src/components/shop/qr-payment-code.tsx` component, wired in `src/app/(shop)/order/[orderNumber]/page.tsx` — shown for bank_transfer orders with status=pending. **Remaining**: QR also in admin order detail view + order confirmation email as inline PNG.
- [ ] [LEAD] Abandoned cart recovery email flow (NEW C40, **DATA VALIDATED C1487, CZ COPY C2293/Scout8**) — 3-email sequence via Resend for abandoned checkouts. Fashion has 84.61% cart abandonment — HIGHEST of all industries. **C2293/Scout8 — FULL CZ COPY FRAMEWORK**: Email 1 (30-60 min): Subject "Stále tě zajímá [Název]?" — product image + "Existuje jen jeden kousek. Kdokoliv ho může koupit dřív než ty." NO DISCOUNT in Email 1. Email 2 (24h): Subject "Stále na tebe čeká... ale ne nadlouho." — personal curation angle "Tohle oblečení jsme osobně zkontrolovali a vybrali. Je to unikát." Email 3 (72h): Subject "Poslední upozornění — [Název] hledá novou majitelku." — "Pokud se rozhodneš dnes, objednáš ho v klidu." **NO DISCOUNT for unique items** (undermines brand trust for curated second-hand). Exception: if item >7 days old and still unsold, 10-15% off acceptable in Email 3. **Sold-item variant** (when item sells before Email 2 fires): "Bohužel, [Název] už má novou majitelku. Ale..." + 3-4 curated alternatives by category/size/price. "Tohle se u nás stává — každý kousek je unikát." **GDPR**: abandonment emails require prior marketing consent — add checkbox to checkout Kontakt section. 3-email sequences = **6.5x more revenue** vs single email. Top performers: 10-14% recovery. 77% of converters act within 1st HOUR.
- [ ] [LEAD] Google Merchant Center free product listings (NEW C40) — register in Google Merchant Center for free organic listings on Google Shopping tab. No ad spend required. Prerequisite: enriched JSON-LD with shippingDetails + hasMerchantReturnPolicy (TODO item #6 in priority). Required product attributes: ID, title, description, link, image_link, availability, price, condition. Fashion extras: color, size, material, brand. Massive free visibility channel.
- [x] [LEAD] Product page sticky CTA + size info above fold — DONE: (1) Sticky mobile CTA — Cycle #1489 (IntersectionObserver + fixed bottom bar). (2) Size guide — Cycle #1485 (modal with EU charts, linked next to size selector). Baymard: 62% of mobile fashion sites have mediocre PDP UX — we're ahead of the curve.

## Phase 8b: Analytics & Tracking [NEW — LEAD RESEARCH C46]
- [x] [LEAD] GA4 + e-commerce conversion tracking — DONE Cycle #2298. `src/lib/analytics.ts` unified dispatcher, `src/components/analytics-provider.tsx` loads scripts consent-gated (GA4=analytics, Pinterest+Meta=marketing), 4 events wired at key touchpoints (view_item PDP, add_to_cart, begin_checkout, purchase). **Remaining**: Pinterest Business account validation + Google Merchant Center registration (free listings).
- [x] [BOLT] GA4 integration — DONE Cycle #2298. Dynamic gtag.js load on analytics consent, all 4 e-commerce events (view_item, add_to_cart, begin_checkout, purchase) dispatched via unified `analytics.ts`.
- [x] [BOLT] Facebook/Meta Pixel — DONE Cycle #2298. Loads on marketing consent, 4 standard events wired (ViewContent, AddToCart, InitiateCheckout, Purchase). Uses `src/components/shop/track-purchase.tsx` for purchase event on order confirmation.
- [ ] [LEAD] **Pinterest Business account + Rich Pins validation (NEW C2298, CONFIRMED AVAILABLE CZ C2304)** — Pinterest Tag is now live (C2298). **C2304 CONFIRMED**: Pinterest Business + Shopping features ARE available in Czech Republic (business.pinterest.com/cs/shopping confirmed). **11th consecutive scan: zero CZ second-hand competition on Pinterest.** Next steps: (1) Create Pinterest Business account at business.pinterest.com, (2) Run Rich Pins validator at `developers.pinterest.com/tools/url-debugger/` — paste any product URL, (3) Pinterest auto-populates price/availability/title from our existing Product JSON-LD (no extra meta tags needed), (4) Apply for Rich Pins approval, (5) Consider Pinterest Catalog/Shopping feed for shoppable pins (requires product feed XML similar to Heureka). "Vintage autumn aesthetic" +1,074% YoY. FREE organic reach. Do this BEFORE Instagram Shopping — lower effort, higher intent, zero CZ competition.
- [ ] [LEAD] **Google Merchant Center registration for free Shopping listings (NEW C2298)** — Analytics foundation done. Next marketing unlock: Google Merchant Center free listings. Our enriched JSON-LD (shippingDetails + hasMerchantReturnPolicy — done C1491) is the prerequisite. Register at merchants.google.com, connect Vercel domain, submit sitemap. Free organic product listings on Google Shopping tab — no ad spend. Required attributes already in our structured data.

## Phase 8c: Extended Structured Data [NEW — LEAD RESEARCH C46]
- [x] [BOLT] BreadcrumbList JSON-LD on all pages — DONE Cycle #1485 (product detail + catalog pages, Katalog → Category → Product hierarchy)
- [x] [BOLT] Organization JSON-LD on homepage — DONE Cycle #1487 (name, logo, url, contactPoint, sameAs)
- [x] [BOLT] WebSite JSON-LD with SearchAction — DONE Cycle #1487 (SearchAction targets /search?q=, enables Google sitelinks search box)
- [x] [BOLT] FAQPage JSON-LD — DONE Cycle #1487 (3 Q&A pairs on product detail pages: shipping/prices, 14-day returns + warranty, quality guarantee + Vinted differentiator)

## Phase 10: AI & Compliance [UPDATED — LEAD RESEARCH C31, C40]
- [ ] [LEAD] EU AI Act compliance for devChat — if devChat is visible to consumers (not just admin), it MUST be labeled as AI interaction. Add "Odpovídá AI asistent" badge in chat header. Effective 2026. Penalty: significant. If devChat stays admin-only → no action needed.
- [ ] [LEAD] Delivery deadline tracking — Czech law requires delivery within 30 days of contract unless agreed otherwise. Add `expectedDeliveryDate` to Order model (set on payment confirmation = now + delivery estimate). Show on order confirmation page + email. Track compliance in admin dashboard.
- [ ] [LEAD] Social commerce features (C31 trend research) — platforms with social/UGC features see 40% higher engagement. Phase 1: "Sdílej na Instagram" button on product detail (generates shareable card image). Phase 2: customer photo reviews (bought + styled). Phase 3: "Právě koupila" feed (anonymous, shows recent purchases with city). Aligns with Gen Z 2.5x faster resale adoption.
- [ ] [LEAD] Google UCP preparation (NEW C40) — Google Universal Commerce Protocol (March 2026) is an open API standard enabling AI shopping agents to browse catalogs, add to cart, and checkout directly. Published `/.well-known/ucp` JSON manifest with merchant capabilities. Currently supported by large retailers (Shopify, Etsy, Wayfair). For Janicka: NOT needed for launch, but complete Schema.org JSON-LD (shippingDetails, hasMerchantReturnPolicy, full product attributes) is the FOUNDATION that UCP builds on. Monitor for small merchant adoption. When UCP matures: implement 3 REST endpoints (session create, update, complete) + JSON capability manifest.
- [x] [LEAD] Product page video support (C40, **UPGRADED C1478 — #1 conversion lever, SPEC CONFIRMED C2298/Scout9, HOSTING SPEC FINALIZED C2304**) — DONE Cycle #2305. — Pages with video achieve **4.8% CR vs 2.9% without** (+65% uplift). Add-to-cart **+144%** after watching video. Returns **-35%**. **C2298 Scout9 SPEC**: (1) **TAP-TO-PLAY only** (NO autoplay) — Google INP requirement ≤200ms, autoplay blocked by mobile browsers. Strong thumbnail with play overlay (poster image doubles as 5th product image). (2) **Format**: 9:16 portrait, MP4 ≤30MB (720p, ~30s). Content structure: 3s front static → 2s back → 5-8s walking/movement (THE conversion shot) → 3-5s fabric detail. (3) **Vinted has NO video support** — immediate differentiator. **C2304 HOSTING DECISION**: Two valid options — (A) **UploadThing** (already integrated, $25/month covers images+video, `f(["video/mp4"], { maxFileSize: "50MB" })` — no HLS/adaptive streaming, adequate for short phone clips, zero new infra) OR (B) **Cloudflare Stream** ($2-3/month extra for 100 videos, includes HLS adaptive streaming + global CDN + analytics — better quality at scale). **Recommendation for launch: UploadThing option A** (zero new accounts/infra, already on $25/month plan). Add Cloudflare Stream post-launch when video library grows. (4) **Implementation**: add optional `videoUrl String?` field to Product model, add `video` upload to UploadThing file router, `ProductVideo` component (`useState playing`, poster overlay → `<video autoPlay controls playsInline>` on tap), render as first gallery slot if present. Admin: video upload in ImageUpload component. (5) **Phone + tripod + natural light** is all that's needed — ~500 CZK total cost. For launch: start with phone clips for hero items (šaty, flowing fabrics).
- [x] [BOLT] **Product video: Prisma model + admin UI + PDP component** — DONE Cycle #2305. `videoUrl String?` on Product model, `productVideo` UploadThing endpoint (32MB, 1 file), video upload UI in admin form with preview+delete, tap-to-play video slide in ProductGallery (last slide, Play button overlay, video thumbnail), PDP passes videoUrl to gallery. — `productVideo` endpoint added to `src/app/api/uploadthing/core.ts` (32MB max, 1 file). Now need the full stack: (1) **Prisma**: add `videoUrl String?` to `Product` model in `schema.prisma`. (2) **Admin**: add video upload section to product edit form using `useUploadThing("productVideo")` — show current video preview if set, upload button, clear button. Place BELOW image uploader in admin product form. (3) **PDP component**: `src/components/shop/product-video.tsx` — `useState(playing)`, static poster (first product image) with centered play button overlay when `!playing`, on click: `setPlaying(true)` → renders `<video autoPlay playsInline controls src={videoUrl} />`. Renders as FIRST item in gallery carousel if `videoUrl` is set. (4) **Migrate**: run `npx prisma migrate dev --name add-product-video` locally, push to Turso via postinstall. (5) **Note**: UploadThing core.ts has 32MB limit — CLAUDE.md spec says 50MB. Update to 50MB in core.ts when implementing.
- [ ] [BOLT] **VideoObject JSON-LD on product detail pages (NEW C2306 — +22% CTR in rich results)** — When a product has `videoUrl` set, nest a `VideoObject` inside the existing Product JSON-LD in `src/lib/structured-data.ts`. Add `videoUrl?: string` and `videoCreatedAt?: Date` to `ProductForSchema` interface. In `buildProductSchema()`, conditionally add: `{ "@type": "VideoObject", "name": "${product.name} — video", "description": "Záběry pohybu a detailů: ${product.name}", "thumbnailUrl": firstProductImageUrl, "uploadDate": videoCreatedAt.toISOString(), "duration": "PT25S", "contentUrl": videoUrl }`. Expose `videoUrl` and `createdAt` from product detail page query. Google requires: `name`, `description`, `thumbnailUrl` (≥1200px), `uploadDate`, `contentUrl`. Confirmed +22% CTR uplift for pages with video rich snippets.
- [ ] [LEAD] "Similar items" product recommendations (NEW C40 — Scout research) — DIY approach for unique-item catalog (qty=1, traditional collaborative filtering fails). Use OpenAI/local embeddings to vectorize product attributes (brand + category + color + size + condition + price range), store in Upstash Vector (free tier, works on Vercel). Nearest-neighbor "Mohlo by se ti líbit" section on product detail page. Also powers "sold item → show alternatives" in abandoned cart emails. Cost: $0 on free tier at our volume. Implementation: 1-2 dev cycles. Alternative: Gorse (open-source Go recommender) or Raccoon (Node.js + Redis).
- [ ] [LEAD] Brand-specific size guide with measurements (NEW C40, UPDATED C46) — 52% of fashion e-commerce returns are due to poor sizing. For second-hand: return = lost sale PERMANENTLY. Build per-brand measurement database: "Zara 38 = prsa 88cm, pas 72cm, délka 95cm". Store in DB, display on product detail ABOVE fold. Crowd-source data from products as they're listed by admin. **C46 UPDATE**: AI size tools now SMB-accessible — Measmerize (ML + SKU-level data, accurate from day 1, affordable tiers), digital-inches approach (basic questions → body estimation → garment matching) gaining traction. AI sizing delivers **3-9x conversion lift** and **25-40% return reduction**. Evaluate Measmerize or similar for post-launch integration.
- [ ] [LEAD] Post-purchase branded tracking experience (NEW C46) — 85% of shoppers avoid a retailer after poor delivery. Customers with good post-purchase experience spend **140% more** over time. Improve order page `/order/[orderNumber]` with: (1) visual shipment status timeline (ordered → confirmed → shipped → delivered), (2) estimated delivery date display, (3) "Podívej se na další kousky" product recommendations (cross-sell on tracking page), (4) review prompt after delivery. One lifestyle brand reduced delivery support tickets from 37% to 4% with branded tracking. Post-purchase = the most neglected conversion lever in fashion e-commerce.
- [ ] [LEAD] **Genlook VTO evaluation (NEW C2293/Scout8 — FREE tier available)** — 2D AI Virtual Try-On now accessible to small merchants. **Genlook** (Shopify-native, but has API): Upload standard product photos → AI generates on-model images. **Free tier: $0-$99/month**. VTO users convert at **2.7x higher rates**, returns drop ~40%. Standard phone-quality photos work — no 3D assets needed. For second-hand: primary objection is "will it fit/look good on me?" — VTO directly addresses this. **Action**: Evaluate Genlook free tier — upload 5-10 hero products, check output quality with Czech body types. **Also evaluate**: Looksy (similar approach). If quality acceptable → add to PDP as secondary image or dedicated tab. Compare with simple `fitNote` field (already in TODO) — VTO is higher effort but higher impact.
- [ ] [LEAD] Simple loyalty/store credit for repeat customers (NEW C46) — Fashion brands average 18% repeat purchase rate, achievable 25%+ with focused effort. Repeat customers spend **67% more** per order. Loyalty participants make **2.5x more purchases**. For second-hand: sustainability-tied credits resonate with Gen Z ("Přineš starý kousek, dostaneš kredit na nákup"). H&M, Patagonia Worn Wear, COS all tie loyalty to circular commerce. Start simple: store credit on 3rd purchase, referral bonus. No complex points system — just honest value for returning customers.
- [x] [LEAD] Heureka XML product feed generator — DONE (api/feed/heureka/route.ts, category mapping, shipping prices, condition labels, revalidates hourly). **Remaining**: GPSR tags (MANUFACTURER_POSTAL_ADDRESS, MANUFACTURER_ELECTRONIC_ADDRESS) — add when manufacturer contact data available in product model.
- [x] [BOLT] Google Merchant Center XML product feed — DONE Cycle #2310. `/api/feed/google-merchant` — RSS 2.0 with g: namespace per GMC spec. Required + recommended apparel attributes (color, size, gender, brand, google_product_category, product_type), shipping details, video_link for products with video, sale_price for discounted items, identifier_exists=false for unique second-hand, up to 10 additional_image_link. **Next step**: register at merchants.google.com → connect domain → submit feed URL for free Shopping listings.
- [ ] [LEAD] **Google Search Console org-level shipping & returns setup (NEW C2311 — 30 min, ZERO coding)** — Google's Nov 2025 update lets merchants configure org-level shipping and return policies once in Search Console — no per-product JSON-LD modifications needed. Search Console settings OVERRIDE structured data. Our existing per-product `shippingDetails` + `hasMerchantReturnPolicy` JSON-LD (done C1491) is good, but Search Console config AMPLIFIES it and is prerequisite for full Google Merchant Center eligibility. **What this unlocks**: (1) Shipping info shown in organic SERP snippets, (2) Returns policy in SERP, (3) Shopping tiles on Google for free. **Action**: (1) Verify Search Console domain property is verified for janicka-shop.vercel.app, (2) In Search Console → Shopping experience → Shipping → add flat rate 69 CZK (under 1500 CZK), free shipping above 1500 CZK, estimated delivery 2-5 business days, (3) Returns → 14 days, full refund, (4) Cross-check GMC feed URL at merchants.google.com. **30 minutes total.** No code changes needed. High SEO/discovery ROI.
- [ ] [LEAD] **Pinterest Business account + catalog submission (NEW C2311 — final step, feed is ready)** — Pinterest Tag installed (C2298) ✅. Pinterest Catalog TSV feed built (C2302) ✅. Only missing: (1) Create Pinterest Business account at business.pinterest.com, (2) Apply for Rich Pins via developers.pinterest.com/docs/rich-pins/ validator (paste any product URL — existing JSON-LD auto-qualifies), (3) In Pinterest Ads Manager → Catalogs → connect our feed URL `/api/feed/pinterest`. Retailers with catalogs see **+30% more checkouts**. Gen Z 50%+ of Pinterest users. Zero CZ second-hand competition (12th consecutive scan). This step is ZERO dev work — business setup only.

## Phase 10c: EU Regulatory & Market Opportunity [NEW — LEAD RESEARCH C1484]
- [ ] [LEAD] EU customs duty messaging — anti-Shein/Temu competitive advantage (NEW C1484) — **From July 1, 2026**: EU removes €150 customs duty exemption. ALL low-value parcels from outside EU get **€3 flat customs duty** per item. Impact on Shein/Temu: **+15-50% price increase** for CZ consumers. 91% of sub-€150 imports came from China in 2024. Over 4.6 billion such parcels entered EU. This is a MASSIVE messaging opportunity for domestic second-hand: "Nakupuj lokálně — bez cla, bez čekání, bez překvapení." Add to: (1) homepage trust section, (2) sustainability messaging, (3) marketing copy. Timeline: messaging ready by June 2026, before July 1 enforcement. Shein/Temu already raising prices — some CZ consumers will actively seek domestic alternatives.
- [ ] [LEAD] Mobile filter UX refinements (C1484 research update) — Additional findings: (1) Size, color, price MUST be the first three filter facets in fashion (confirmed by multiple 2026 studies). (2) **76% of major e-commerce sites** have filter usability issues serious enough to cause abandonment. (3) Swipe-down gesture to dismiss filter overlay is critical (mirrors physical "pushing away" — faster than hunting for X button). (4) Auto-close overlay after "Zobrazit X produktů" tap (300ms ease animation). Already in TODO as priority #1 — these are refinement notes for implementation.

## Phase 10b: Social Discovery & AI Commerce [NEW — LEAD RESEARCH C1478]
- [ ] [LEAD] Social discovery strategy (NEW C1478 — ThredUp 14th Report) — **46% of secondhand discovery** now happens through social feeds, livestreams, and creator curation (NOT search). **48% of shoppers use AI shopping tools.** Janicka must prepare for social-first discovery: (1) shareable product cards optimized for Instagram Stories (image + price + "Unikát" badge), (2) Open Graph rich previews on every product page (already done — verify quality), (3) "Sdílej" button generating Instagram-ready image with product overlay. This is NOT about selling on social — it's about making every product page worth sharing.
- [ ] [LEAD] **Pinterest business account + rich product pins (NEW C2289, CONFIRMED C2293 — ZERO COST, HIGH ROI)** — Pinterest is a high-intent discovery channel (users plan purchases, don't passive-scroll). "Vintage autumn aesthetic" searches +**1,074%** YoY. "Dream thrift finds" +550%. Gen Z is 50%+ of Pinterest users — exactly our target. **ZERO CZ second-hand competition on Pinterest** (confirmed C2289+C2293 scan). **C2293 KEY FINDING**: Pinterest NOW accepts JSON-LD / Schema.org data for Rich Pins — our existing Product JSON-LD is sufficient, no separate Open Graph meta tags needed. One jewelry shop saw +90% pin engagement and +50% direct sales after enabling. Setup: (1) Pinterest Business account, (2) install Pinterest Tag (see [BOLT] item below), (3) validate Rich Pins via `developers.pinterest.com/docs/rich-pins/` validator — paste any product URL, (4) Products auto-populate price/availability/title from our JSON-LD. **Free organic reach with NO ad spend.** Set up BEFORE Instagram Shopping (lower effort, higher intent traffic, no competition from CZ second-hand).
- [x] [BOLT] **Pinterest Tag installation** — DONE Cycle #2298. Loads on marketing consent via `analytics-provider.tsx`, dispatches via unified `analytics.ts` (add_to_cart, begin_checkout, purchase). `NEXT_PUBLIC_PINTEREST_TAG_ID` env var. Pinterest Business account validation is next step (see LEAD item in Phase 8b).
- [x] [BOLT] **Pinterest Catalog feed endpoint `/api/feed/pinterest` (NEW C2293/Scout8)** — DONE Cycle #2302. TSV format, Google taxonomy IDs, condition mapping (new_with_tags→new / rest→used), up to 10 additional_image_link, shipping info included. Revalidates hourly. **Next step**: Pinterest Business account → submit `/api/feed/pinterest` as catalog data source (see LEAD item in Phase 10b).
- [ ] [LEAD] **Instagram Shopping catalog (NEW C2289, UPDATED C2293 — SCALE SOCIAL DISCOVERY)** — **12% more likely to buy clothing on Instagram** than any other platform. **+37% more clicks** from product tags in Reels vs. feed posts. 130M users tap shoppable posts monthly. **NEW C2293 (March 24, 2026)**: Meta announced creator commissions on Instagram Shopping + new buy button embedded within ads — Meta is doubling down on commerce. Format: product in first 3 seconds, 9:16 vertical video, 15-30s optimal, closed captions for silent viewers, max 2 product tags per Reel. Setup: (1) Connect Meta Business account, (2) upload product catalog (adapt Heureka XML feed structure), (3) enable Instagram Shopping review, (4) tag products in every Reel + Story. Meta removed native checkout → Instagram is demand engine (discovery → our PDP converts). **Pair with Pinterest** for dual social discovery: Pinterest = high intent planning, Instagram = impulse discovery.
- [ ] [LEAD] Comgate SDK abstraction layer (NEW C1478, **INLINE GATE SPECS CONFIRMED C2298**) — @comgate/checkout-js remains at **v2.0.15** (13th consecutive check, Nov 2025, no new release). SDK replacement warning still live, no ETA. **C2298 RESEARCH — Inline Gate full spec**: For card payments, use `embedded=true` REST param → iframe at **504px wide × 679px tall** (`<iframe allow="payment" src="[returned url]">`). Trigger with `comgateOpen()`. Two variants: nested (fills cart area) or popup modal. **CSP required**: `frame-src comgate.eu` in Next.js response headers. BNPL + bank transfer always redirect regardless. **Final hybrid strategy**: Apple Pay + Google Pay via SDK, cards via inline iframe, bank transfer + BNPL via redirect. **Action**: wrap all SDK calls in `src/lib/payments/comgate-sdk-wrapper.ts` abstraction (Interface: `initApplePay()`, `initGooglePay()`, `initInlineGate()`, `handlePaymentResult()`). Must be done BEFORE first SDK integration — defensive architecture for inevitable SDK swap.
- [ ] [LEAD] "Právě prodáno" + favorites counter social proof (NEW C1478) — Two ethical urgency patterns validated by research: (1) "Právě prodáno" live feed on homepage showing recently sold items with "Prodáno za X hodin" — proves items sell fast, creates honest FOMO. (2) Favorites/views counter on product detail: "12 lidem se líbí" — social proof that's TRUE (from wishlist data). For second-hand unique items, 31% of consumers more likely to buy limited items, 39% of Gen Z respond strongest. **Anti-patterns to AVOID**: fake countdowns, inflated viewer counts, "Only 1 left!" everywhere (everything is qty=1 — becomes noise). Use REAL data only.
- [ ] [LEAD] Admin photo-to-listing AI assist (NEW C1478, **MARKET VALIDATED C1487**) — Depop's AI listing from photo: one photo generates full listing. Poshmark "Smart List" (2026): photo → AI classification, tag detection, title/description generation. Snap2List: photo → optimized listing in 30 seconds (SEO titles, descriptions, categories, pricing, background removal). **Label recognition tech** now extracts brand, size, material, origin directly from product label/tag photos. For Janicka admin: when uploading product photos, offer AI-assisted field completion — auto-detect brand (from tags), suggest category, estimate condition, generate Czech description. Uses GPT-4o Vision or similar. Reduces admin listing time from 5 min to <1 min. **Market context**: "Listing friction" is THE #1 seller drop-off point across all secondhand platforms. AI removes it — this is now core infrastructure, not a nice-to-have feature. **Critical for scaling**: Janička needs to list items FAST from phone. Post-launch priority but high operational impact.
- [ ] [LEAD] BNPL prominence in checkout (NEW C1478) — BNPL now **7% of CZ e-commerce** (up from 2% in 2021, projected 15%). Increases AOV by **15-20%**, and 30-50% of BNPL purchases wouldn't happen without it. Comgate has native pay-in-3. For items above 500 CZK (lowered from 1000 CZK threshold — data supports broader adoption among younger shoppers), show "3 splátky po X Kč" prominently on product detail AND in checkout payment section. Gen Z + millennials = highest adoption.

## Phase 10d: Search & Email Revenue [NEW — LEAD RESEARCH C1499]
- [x] [BOLT] Instant search: wire into header — DONE (C2293 audit confirmed). `import { InstantSearch } from "./instant-search"` in `header.tsx` line 5, rendered in right-side div. Desktop search ✅. **Remaining**: mobile-nav.tsx still missing — see Phase 2 item "Instant search: wire into mobile-nav.tsx". Site search users convert **2-3x higher**, drive up to **41% of total revenue**. **nuqs v2.8.9** confirmed (no v3).
- [x] [BOLT] **Abandoned cart capture + recovery emails (NEW C2293)** — DONE Cycle #2303 (C2311 Scout confirmed full system live). `captureAbandonedCart()` Server Action on email field blur in checkout. Vercel Cron every 15 min, Email 1 at 45min, Email 2 at 18h, Email 3 at 60h. Sold-item check in Emails 2+3 (`getSoldProductIds()`). GDPR unsubscribe at `/api/unsubscribe/abandoned-cart/[id]`. Order recovery marks cart as `recovered`. Czech copy: "Zapomněla jsi?", "Stále čeká", "Poslední upozornění". **Remaining**: (1) Set `CRON_SECRET` env var on Vercel — cron fails closed without it. (2) Configure verified Resend sending domain for `from:` address.
- [ ] [BOLT] **Cart-page email capture for pre-checkout abandonment (NEW C2311 — Scout finding)** — Current system only captures email when customer TYPES it in checkout Kontakt section. Customers who add to cart and leave before checkout are NEVER captured. Gap: for second-hand unique items, this is significant lost recovery. Add opt-in widget on cart page: "Chceš být upozorněna, pokud někdo koupí tvůj kousek?" with email input + GDPR checkbox. On submission: call `captureAbandonedCart()` with empty/partial cart state. This captures higher-funnel abandonment (cart abandonment rate is 84.61% in fashion). Alternatively: use existing `ProductNotifyRequest` model for per-product interest capture. Architecture choice: cart-level (simpler, matches existing system) vs per-product (more targeted, aligns with unique-item scarcity).
- [ ] [LEAD] Browse abandonment email flow (NEW C1499) — **SEPARATE from cart abandonment**. Targets users who VIEWED products but didn't add to cart (higher funnel than cart abandonment). 2-email sequence via Resend: Email 1 at 2-4 hours ("Zaujal tě tento kousek?"), Email 2 at 24-48 hours (show related products from same category). For second-hand unique items: "Tento kousek si prohlížíš jen ty — ale kdokoliv ho může koupit." **Frequency cap**: max 1 browse abandonment email per week per subscriber. **Requires**: (1) Track product views per email (match newsletter subscriber email to browsing via cookie), (2) Store viewed product IDs + timestamps, (3) React Email template with product image + link. **Data**: Browse abandonment sits at top of conversion funnel — these are warm leads. SMS + email combined: **15-20% higher recovery**. This complements our existing cart abandonment TODO item for a full email recovery funnel.
- [ ] [LEAD] **Authentic scarcity copy framework for ALL email flows (NEW C2289 — +332% conversion)** — **EVERY Janicka item IS unique (qty=1) — this is genuinely true.** Authentic scarcity messages ("only one exists") lift conversion **+332%** per A/B data. 60% of buyers say FOMO influences purchase decisions. All cart abandonment, browse abandonment, and wishlist reminder emails MUST use this honest urgency framework. Copy examples: (1) "Tento kousek je unikát — existuje jen jeden. Kdokoliv ho může koupit dřív než ty." (2) "Nestihla jsi to? Bohužel, [produkt] už našel novou majitelku." (when sold) → redirect to similar items. **Anti-patterns to NEVER use**: fake countdowns, generic "hurry!", inflated stock urgency. Janicka's scarcity is REAL — use it plainly. Brief for all email templates from Bolt: subject line must name the specific product ("Stále čeká tvůj [brand] [type] ve velikosti [size]"), body first sentence states uniqueness factually. Do NOT use email #3 discount to create urgency — use the authentic fact of unique inventory instead.
- [ ] [LEAD] Email marketing foundation — 5 core automated flows (NEW C1499) — Automated email flows generate **41% of all email revenue** with only **5.3% of sending volume**. Revenue per recipient is **18x higher** for flows vs campaigns. Top stores generate **30-40% of total revenue** from email. The 5 foundational flows that generate **80% of email revenue**: (1) **Welcome series** ✅ partially done (newsletter welcome email C1497) — extend to 3-email sequence with product recommendations. (2) **Cart abandonment** — TODO item exists, spec'd as 3-email sequence. (3) **Browse abandonment** — NEW item above. (4) **Post-purchase** ✅ partially done (order confirmation emails C44-45) — extend with review request + "Podívej se na další kousky" cross-sell 5-7 days after delivery. (5) **Re-engagement/win-back** — target subscribers inactive 60+ days with "Chybíš nám" email + curated picks. **Welcome flow benchmarks**: 45-50% open rate, 8-12% conversion to first purchase, 30-50% convert within 30 days. **Cart recovery**: $3.65/recipient avg, top 10% earn $28.89/recipient (8x more). This is THE untapped revenue channel.

## Priority Order (Lead Recommendation — Updated Cycle #1499 Research)
### ✅ DONE (Cycles #22-#1500)
- ~~Image upload~~ (C25) — UploadThing v7
- ~~SEO structured data~~ (C22) — JSON-LD, sitemap, robots.txt, OG
- ~~Pagination~~ (C22) — 12/page
- ~~Product filters~~ — brand, size, condition, price range, category, sort, filter chips
- ~~"Nově přidané"~~ — homepage section + Novinka badge
- ~~Cart reservation~~ (C27) — 15min timer, TOCTOU-safe, countdown
- ~~Cookie consent~~ (C27) — GDPR/ECA compliant, granular categories
- ~~Quick view modal~~ (C1482) — eye icon → Dialog with product details
- ~~Second image hover~~ (C1482) — CSS crossfade on desktop hover
- ~~Sort by discount~~ (C1482) — "Největší sleva" option
- ~~Announcement bar~~ (C1483) — rotating promo messages
- ~~Trust badges + accordion~~ (C1483) — 4-column grid + shipping/returns/quality disclosures
- ~~30-day price history~~ — price-history.ts, Omnibus Directive compliant
- ~~BreadcrumbList JSON-LD~~ (C1485) — product detail + catalog pages
- ~~Size guide~~ (C1485) — modal with EU charts
- ~~Checkout mobile summary~~ (C1485) — sticky bottom bar + expandable sheet
- ~~Category metadata~~ (C1487) — dynamic title/description/canonical per category
- ~~Organization + WebSite + SearchAction JSON-LD~~ (C1487) — homepage
- ~~FAQPage JSON-LD~~ (C1487) — product detail pages
- ~~Cart free shipping progress bar~~ (C1489) — animated bar + "Doprava od 69 Kč"
- ~~Sticky mobile CTA~~ (C1489) — IntersectionObserver + fixed bottom bar
- ~~Delivery estimate~~ (C1489) — business-day calculation + display
- ~~Enrich JSON-LD Golden Record~~ (C1491) — shippingDetails + MerchantReturnPolicy + color/size/condition attributes
- ~~WhatsApp share~~ (C1491) — sharing button + native Web Share
- ~~Smart similar products~~ (C1491) — scored by size/price/brand match
- ~~Size/color indicators on cards~~ (C1494) — size badges + color dots
- ~~Custom 404~~ (C1494) — branded page with search + categories + products
- ~~"Právě prodáno" feed~~ (C1496) — 8 recently sold items + horizontal scroll
- ~~Brand showcase~~ (C1496) — top 12 brands as clickable pills
- ~~Sustainability savings~~ (C1496) — green banner with exact CZK savings
- ~~Touch swipe gallery~~ (C1497) — direction-locked gestures + lightbox swipe
- ~~Quick-add form~~ (C1497) — /admin/products/quick-add
- ~~Newsletter welcome email~~ (C1497) — branded HTML via Resend
- ~~Footer legal links~~ — ODR + ČOI
- ~~Heureka XML feed~~ — api/feed/heureka/route.ts
- ~~Wishlist~~ — localStorage, /oblibene page, header button
- ~~Cart cross-sell~~ (C1500) — "Mohlo by se vám líbit" in cart
- ~~Order tracking lookup~~ (C1500) — /order/lookup
- ~~Admin newsletter management~~ (C1500) — subscriber table + CSV export
- ~~QR bank transfer payment~~ (C2293 audit) — `qr-platba.ts` + `QrPaymentCode` component wired on order confirmation for bank_transfer orders
- ~~Instant search desktop header~~ (C2293 audit) — `header.tsx` imports + renders `<InstantSearch />`

### NEXT SPRINT — Phase 2 Polish + Phase 3 Checkout (UPDATED C2311)
0. **⚠️⚠️ Next.js CVE security patch (UPDATED C2311 — NEW CVE FOUND, CVSS 7.5 HIGH)** (Phase 8, **DO FIRST**) — **THREE CVEs total, not two.** CVE-2026-27979 + CVE-2026-29057 already fixed in 16.2.2 (we're past those). **NEW: CVE-2026-23869** (CVSS 7.5 HIGH) — React Server Components DoS, affects ALL of 16.x including our 16.2.2. Specially crafted HTTP request to ANY Server Function endpoint triggers excessive CPU. Fixed in **16.2.3**. Vercel WAF provides partial mitigation only. **CORRECT FIX: `npm install next@16.2.3 eslint-config-next@16.2.3`** — `npm update next` will NOT work because version is pinned as `"next": "16.2.2"` (no caret in package.json). Must explicitly install 16.2.3. No breaking changes — pure security + bug fix release.
0b. **shadcn/ui Radix migration** (Phase 8, **C1511 — QUICK WIN**) — `npx shadcn@latest migrate radix "src/components/ui/**"`. Consolidates ~15 @radix-ui packages into one. 5 minutes.
1. ~~**Mobile filter drawer**~~ ✅ DONE (C2308) — full-screen shadcn Drawer, facet order (Size → Price → Color → Brand → Condition → Sale), active count badges, iOS safe-area, swipe-down to close.
2. **Promoted quick filters** (Phase 2, **NOT YET BUILT** — falsely claimed in C2286) — horizontally scrollable chip bar above product grid (size pills + color swatches). Implement in `QuickFilters` component reading `searchParams`. 61% of sites don't promote filters (Baymard).
2b. ~~**Instant search: desktop header**~~ ✅ DONE (C2289 verified — `header.tsx` already imports + renders `<InstantSearch />`). **Remaining: mobile-nav.tsx** — add InstantSearch trigger in mobile hamburger menu. 20-min task.
3. **`nuqs` adoption** (Phase 2) — v2.8.9 confirmed stable for Next.js 16 (no v3). Type-safe URL params, debounced price input, server cache. Eliminates ~50 lines of manual parsing.
4. **Color filter + filter counts** (Phase 2) — color swatches + product count per option + grey out zero-result options.
5. ~~**Instant search — desktop header**~~ ✅ DONE (C2289 verified). **Remaining: mobile-nav** — wire `InstantSearch` into `mobile-nav.tsx`. NOTE: Czech diacritics correctly handled via custom `processTerm` in component. **nuqs v2.8.9** confirmed stable for Next.js 16 (no v3).
6. **Server-side cart validation** (Phase 3) — `validateCart()` Server Action at checkout initiation. Never trust client cart.
7. **Accordion checkout + Packeta** (Phase 3+6, UPDATED C2293/Scout8) — 4 sections: Kontakt → Doprava → Platba → Shrnutí. Express Apple Pay/Google Pay ABOVE accordion (2x mobile CR). Collapsed sections MUST show readable summaries: "Jana Nováková • jana@email.cz" / "Zásilkovna — Výdejna Praha 1, Vodičkova 32" / "Apple Pay". Single name field. ≤7 fields. Max 3 trust badges inline below payment (Lock + Comgate + Zásilkovna). **Address autocomplete**: use **Mapy.com API** (250k credits/month FREE, best CZ coverage, developer.mapy.com) NOT Google Places. **Packeta widget**: load `library.js` via `next/script strategy="afterInteractive"` on page load. Call `.pick()` ONLY when Doprava section expands + Packeta selected. **GDPR marketing consent checkbox** in Kontakt section (required for cart abandonment emails). Shopify mandating one-page for all merchants by Aug 2026.
8. **Comgate payment** (Phase 3) — wrap SDK in abstraction layer. Apple/Google Pay inline, card via iframe, bank/BNPL redirect. **Easy plan now 0.9%** (price drop). SDK still v2.0.15 (**12th consecutive check** — no update).
9. ~~**QR code payment**~~ ✅ DONE (C2293 audit) — wired in order confirmation for bank_transfer. **Remaining**: admin order detail view + order email inline PNG.
10. **Email marketing foundation** (Phase 10d, **C1499 — HUGE UNTAPPED**) — 5 core flows = 80% of email revenue. Welcome, cart abandonment, browse abandonment, post-purchase, win-back. **C1511**: Layer WhatsApp recovery on top post-launch (80% open rate, 5x ROI).
11. **Abandoned cart recovery emails** (Phase 9) — 3-email sequence. Fashion = 84.61% abandonment. 6.5x revenue vs single email. **C1511**: Multi-channel (email + WhatsApp) = 2-3x higher engagement. **C2293**: `AbandonedCart` model EXISTS in Prisma — needs capture logic at checkout Kontakt step + cron job. Straight subject lines convert best ("Tvůj [Brand] [item] čeká" = 32.73% conversion, scarcity in body = +200% CTR).
12. **Browse abandonment email** (Phase 10d, **C1499**) — higher-funnel recovery for product viewers who didn't add to cart.
12b. **⏰ TIME-SENSITIVE: Vinted T&C April 30 messaging** — prep Instagram Stories copy + homepage trust update for April 28–May 1 window. Angles: (1) AI training data clause — perpetual non-opt-out license for user photos, (2) AI-generated photos explicitly allowed on Vinted (buyers can't tell real from AI), (3) Vinted.cz Trustpilot 2.1/5 (76% one-star). Janicka copy: "Tvoje fotky jsou tvoje. Nikdy je nepoužijeme k trénování AI." **21 days away (April 9 → April 30).** This is the strongest competitive messaging window of Q2 2026.
12c. **Google Search Console shipping/returns setup (NEW C2311)** — 30 min, zero coding, unlocks Google Shopping snippets + Merchant Center eligibility. See Phase 8c item.
12d. **Pinterest Business account + catalog submission (NEW C2311)** — zero dev work, feed is ready. See Phase 8c item. Do BEFORE Instagram Shopping.

### LAUNCH BLOCKERS
12. ~~Cookie consent~~ ✅ DONE
13. ~~30-day price history~~ ✅ DONE
14. **Rate limiting** (Phase 8) — @upstash/ratelimit for checkout + login. Flagged every Trace cycle since C19.
15. ~~EAA accessibility~~ **EXEMPT** (micro-enterprise)
15b. **⚠️ Next.js CVE-2026-23869** (C2311 — CVSS 7.5 HIGH, new finding) — React Server Components DoS in ALL 16.x including our 16.2.2. Fixed in 16.2.3. Fix: `npm install next@16.2.3 eslint-config-next@16.2.3`. Vercel WAF is NOT full protection. Must patch before deploy.

### POST-LAUNCH (UPDATED C1505)
16. **Email notifications** (Phase 6) — ✅ MOSTLY DONE. Remaining: PDF invoice, abandoned cart sequence.
17. ~~**Product video**~~ ✅ DONE (C2308) — `videoUrl String?` on Product model, UploadThing endpoint, tap-to-play PDP gallery slide, admin upload UI. VideoObject JSON-LD also DONE (C2309 Trace + C2310 Bolt standalone schema).
18. **Heureka.cz verification** (Phase 9) — XML feed ✅ DONE. Register for free Start tier. Add GPSR tags when manufacturer data available.
19. **Google Merchant Center free listings** (Phase 8) — JSON-LD ✅ DONE. GMC XML feed ✅ DONE (C2310). **Remaining**: register at merchants.google.com, connect domain, submit feed URL. See Sprint item 12c for Search Console config.
20. **GA4 + conversion tracking** (Phase 8b) — ZERO analytics connected. CRITICAL before any paid marketing.
21. ~~Extended structured data~~ ✅ DONE (BreadcrumbList, Organization, WebSite, FAQPage — all shipped C1485-C1487).
22. **PWA manifest + service worker** (Phase 3) — Fashion PWAs: 70% higher conversion. 169% increase (Butcher of Blue).
23. **Social discovery** (Phase 10b) — 46% of secondhand discovery is social. Shareable cards, OG verification.
24. **Post-purchase tracking** (Phase 8) — Order tracking lookup ✅ DONE. Extend with status timeline + recommendations.
25. **Curated collections** (Phase 2+9) — editorial themed groups. Vinted Collections now PAID — ours should be free.
26. **Scarcity UX** (Phase 2) — "Unikátní kus" badges + favorites counter (real wishlist data).
27. **EU customs duty messaging** (Phase 10c) — July 1, 2026: €3 duty on all imports. Ready by June.
28. **Loyalty/store credit** (Phase 9) — 67% more spend from repeat customers.
29. **Accessibility incremental** (Phase 7c) — competitive advantage, not legal requirement.
30. **Social commerce** (Phase 10) — share buttons, UGC video reviews, "Právě koupila" feed.
31. **Instagram Shopping** (Phase 9) — product feed + micro-influencer partnerships. **54% of Gen Z say social better than search for discovery** (C1505).
32. **Saved search alerts** (Phase 9) — biggest differentiator vs Vinted.
33. **Admin AI listing assist** (Phase 10b) — photo-to-listing AI. Critical for scaling.
34. **Zalando Pre-Owned monitoring** (UPDATED C1511) — live in CZ, 50% sells within 24h. **Expanded to kids' fashion** (Feb 2026, 14 markets) — signals strategic commitment. Adult pre-owned sales doubled. Monitor pricing overlap with our target segment.
35. **Google UCP preparation** (Phase 10) — monitor for small merchant adoption.
36. **middleware.ts → proxy.ts migration** (Phase 8) — Next.js 16 deprecated middleware. Codemod: `npx @next/codemod@latest middleware-to-proxy`. Low effort.
37. **WhatsApp cart recovery** (Phase 10d, **NEW C1511 — HIGH ROI**) — 80% open rate, 5x ROI. Layer on top of email flows post-launch.
38. **"Holy grail hunting" messaging** (Phase 8, **NEW C1511**) — Gen Z shifted from hauls to curated finds. Align messaging: "Každý kousek je originál."
39. **Prisma 7 migration** (Phase 8, **NEW C1511 — POST-LAUNCH**) — ESM-only, new config, driver adapters required. Substantial migration. Plan as dedicated task after launch.
40. **AI VTO evaluation** (Phase 10, **NEW C1511**) — Genlook: $0-99/mo, +35% ATC, Shopify-native only. For non-Shopify (Next.js): API-based VTO solutions emerging but unproven for second-hand irregular sizing. Monitor for Next.js-compatible options.

## Competitive Positioning (Lead Research C19, UPDATED C34, C37, C40, C1478, C1487, C1493, C1499, C1511)
- **Closest competitor**: MegaSecondHand.cz (women-focused, ~3,500 curated pieces). Body visualization still gradual, slow pace. **C1478**: now surveying users about VTO (user-uploaded photos) — ambitious but not shipped. If Janicka launches with consistent on-body photography, we maintain the lead. **C1487**: No new features detected. Still advertising curated collection refreshes. **C1511**: No changes detected.
- **Largest**: Brumla.cz (**C1478 UPDATE**: 57,888 items in stock, 15k new/week). 98% Heureka (7,000+ reviews). **NO mobile app, no AI, no personalization.** Volume-first, zero smart discovery. Their lack of a mobile app is notable — 62% of CZ e-commerce is mobile. Has reservation system (hold 2 weeks). **C1487**: Now advertising 20,000+ items with new items added every Monday and Thursday at 8AM, shipping from 45 CZK. Still no personalization or AI features. THREAT: LOW for our curated segment. **C1511**: No changes detected.
- **Other CZ second-hand (NEW C1511)**: Semoda.cz (from 1 CZK, ultra-cheap volume), Unimoda.cz (since 2004, generic branded), second-hand.cz (claims "largest" CZ SH), Dosekace.cz (wholesale + retail, running 2026 clearance sales). None target curated women's 18-35 with modern UX. **10th consecutive scan: NO new dedicated competitors.**
- **Vinted** (C1499 UPDATE): **EUR 8B valuation** (BlackRock, Ontario Teachers' acquiring stakes — up from EUR 5B). Vinted Pay wallet live in 8 EU countries + **now in Slovakia** (CZ neighbor — CZ rollout possible 2026) — **CZ still NOT included.** Collections now **PAID** (sellers pay for placement). **AI CRISIS NOW INTERNATIONALLY RECOGNIZED (C1496)**: OECD catalogued GenAI refund fraud on Vinted as AI incident (March 2, 2026). **C1499 TRUSTPILOT DATA**: Vinted.cz Trustpilot score = **2.1/5**, with **76% one-star reviews**. Top complaints: (1) AI-automated account bans with no human review, (2) customer support described as "useless" with only copy-paste responses, (3) buyer protection fee perceived as ineffective, (4) country-locked accounts blocking withdrawals. CZ users on VašeStížnosti.cz threatening escalation to ČOI + European Consumer Centre. **Messaging opportunity at ALL-TIME PEAK**: "Na Vintedu nevíš, co kupuješ. U nás ano. A navíc ti nezablokujeme účet."
- **Depop (UPDATED C1499)**: **eBay acquiring for $1.2B** — deal NOT yet closed (announced Feb 2026, regulatory approval pending, expected Q2 2026). 7M buyers (90% under 34), $1B GMS. Key features: AI photo-to-listing, "Outfits" feature. eBay plans to keep Depop as standalone brand, **cross-listing features** (Depop social feeds → eBay global search) expected by **late 2026**. RELEVANCE for CZ: LOW directly, but cross-listing could eventually expand Depop's reach into EU markets via eBay infrastructure.
- **CZ online fashion landscape (C37, UPDATED C1484)**: Zalando 42%, About You 40%, Zoot 26%. Shein/Temu gaining fast-fashion share BUT **EU customs duty exemption removal (July 1, 2026)** hits them hard: €3 flat duty on ALL sub-€150 parcels. Consumer price increase of 15-50%. 91% of sub-€150 imports came from China. Over 4.6B such parcels entered EU in 2024. This structurally weakens ultra-cheap imports and pushes CZ consumers toward domestic alternatives including second-hand.
- **No new CZ competitors** — **8th consecutive scan (C1499)** confirming market gap in curated women's second-hand. Odkarla.cz exists (C2C, generic, not curated).
- **ThredUp 14th Resale Report (NEW C1478 — published April 2, 2026)**: Global secondhand grew 13% (new apparel flat). **34% of clothing budget** now secondhand. **46% browse resale BEFORE buying new.** 60% factor resale value in new purchases. AI: +30% active buyers, +27% orders. **48% use AI shopping tools, 63% comfortable with agentic buying.** Supply is now the constraint, not demand.
- **Product video = #1 conversion lever (NEW C1478)**: +65% CR, +144% add-to-cart, -35% returns. Even phone clips work. UGC video: up to +74% conversion.
- **Cart abandonment crisis (C40)**: Fashion 84.61% abandonment. One-page checkout -20%. Apple/Google Pay + guest checkout = 20-35% mobile improvement. 39% abandon due to unexpected costs.
- **Janicka differentiator**: premium curation, Instagram-aesthetic UX, guaranteed quality, pro photos, on-body photography, fast single-warehouse shipping, **trust narrative amplified by Vinted crisis**
- **Key message**: "My jsme to už zkontrolovali, aby ses nemusela."
- **Anti-pattern**: NO fake urgency. Use REAL scarcity (every item IS the last one), REAL social proof (wishlist counts, recently sold). Sustainability-conscious 18-35 crowd hates manufactured urgency.
- **Page speed target**: Sub-2.5s LCP. Every additional second: +20% bounce rate.
- **Touch targets**: min 48x48px (C1478: targets under 48px = 60% more tap errors).
- **Mobile grid**: 2 columns, thumb-friendly, bottom nav bar
- **CZ market size (UPDATED C1499)**: CZ e-commerce market = **$8.42B in 2026**, growing at **4.91% CAGR** → $10.7B by 2031 (Mordor Intelligence). Mobile = **62.35% of transactions**, growing at 6.85% CAGR. 5G coverage at national scale (including Prague Metro) enables seamless mobile experiences. **BNPL** (UPDATED C1499): **5% of CZ online sales** (lower than our C1478 figure of 7% — likely different measurement methodologies), expanding at **8.1% CAGR**, increases basket sizes by up to **35% for fashion**.
- **Site search impact (NEW C1499)**: Site search users convert **2-3x higher** than non-search users. Site search can drive up to **41% of total revenue**. Autocomplete on 82% of top 50 e-commerce sites but 36% do more harm than good (bad implementation). Janicka currently has NO autocomplete — significant gap. For fashion vocabulary: synonym mapping is critical ("šaty" vs "saty", brand name variants, condition terms).
- **Email as revenue channel (NEW C1499)**: Automated email flows generate **41% of all email revenue** with only **5.3% of sending volume**. RPR (revenue per recipient) is **18x higher** for flows vs campaigns. Cart recovery: avg **$3.65/recipient**, top 10% earn **$28.89/recipient** (8x more). Welcome flows: **45-50% open rate**, **8-12% conversion** to first purchase. **5 core flows** = **80% of email revenue**: welcome, post-purchase, browse abandonment, cart abandonment, checkout abandonment. Top stores generate **30-40% of total revenue** from email + SMS.
- **Conversion benchmarks (UPDATED C1499)**: Fashion avg 2.9-3.3% CR. **Women's apparel: ~3.6%** (higher than general fashion — Shopify 2026 data). Mobile: ~1.5-2.2% vs desktop ~3.5-4.3%. **Only 38% of mobile sites have decent or better PDP UX** (Baymard 2026). **Product images**: 4-7 purposeful images recommended (front, back, close-up detail, fabric, on-body, size label — NOT 20 identical angles). **Product video: +65%.** On-model photo: +33%. Express payments: **Apple Pay = +22.3% conversion, +22.5% revenue** (Stripe 150k+ sessions). **Apple Pay + Google Pay: now 30% of ALL online transactions** (accelerating). Express at top of checkout: **2x mobile conversion** vs bottom. Digital wallets: **49-56% of global e-commerce value**. BNPL: +15-20% AOV. Trust signals **inline with payment**: +18% completion (**40-60% better** than footer placement, -32% abandonment). Products with 11-30 reviews: +68%. 62% of mobile fashion sites have mediocre PDP UX. **Gesture-based galleries: +108% product views** (H&M: 2.3x, C1493). Sticky mobile CTA: **+10-25%**. Baymard: 90% fail size/fit. **Address autocomplete: -35% abandonment, -30% mobile checkout time** (ASOS: 62s→8s, +12% transactions). **Checkout fields**: optimal 7-8 (each extra = -4-6% completion). **Guest checkout: +45% conversion** vs forced registration (26% abandon at forced signup). Free shipping progress bar: **+15-20% AOV**. Abandoned cart 3-email: **6.5x revenue** vs single email.
- **AI shopping imperative (NEW C1487)**: Google AI Overviews CTR drops **61%** — being cited > ranking #1. "Golden Record" (99.9% attributes) = 3-4x AI visibility. ChatGPT Shopping: structured data quality > traffic volume. OpenAI ending Instant Checkout → retailer apps. Both platforms prioritize Schema.org — JSON-LD enrichment is now TWO-PLATFORM imperative.
- **Accessibility as differentiator (C37, C40)**: EXEMPT from EAA (micro-enterprise). Still competitive advantage — ZERO CZ second-hand competitors are WCAG-compliant.
