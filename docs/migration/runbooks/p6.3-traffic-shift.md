# P6.3 — Gradual LB traffic shift (Hetzner 10% → 50% → 100%)

**Task:** #343 — shift production traffic off Vercel onto Hetzner in three weighted stages, verifying error rate and p95 latency stay ≤ Vercel baseline at each step.
**Target:** after 72h cumulative at 100% Hetzner, Hetzner is primary-only and Vercel is failover-only (the P6.1 end-state).
**Depends on:** P6.1 LB live (#341), P6.2 failover drill passed (#342).
**Status:** BLOCKED on the dependency chain above + on domain purchase (#329). Runbook is ready-to-execute; no code change needed.

---

## Prerequisites (all must be green before stage 1)

1. **P6.1 LB is live** — `curl -sS https://janicka-shop.cz/api/health` returns Hetzner's `commit` sha (primary) and CF dashboard shows both pools HEALTHY.
2. **P6.2 failover drill passed** — the 60s failover window, recovery, and no-524 window were all validated. Evidence: screenshot or commit note in `docs/migration/hetzner-all.md` progress log.
3. **Hetzner Vercel parity checked** — `VERCEL_GIT_COMMIT_SHA` vs Hetzner `GIT_COMMIT_SHA` match current `main` head. If they differ, either redeploy Hetzner (`pm2 restart janicka-shop`) or wait for Vercel's build to finish. Stage 1 only works if both origins serve identical app code.
4. **Baseline captured** — 24h Vercel-only numbers pulled from:
   - Vercel → Project → Analytics: p50 / p75 / p95 latency, error rate, requests/s peak.
   - Sentry `janicka-shop` project: error count per 24h, unique issue count.
   - Record baseline in this runbook's "Baseline snapshot" section below before starting stage 1.
5. **Rollback rehearsed mentally** — single dashboard action reverses each stage (adjust pool weights back). Know which page/field to click before touching anything.
6. **Low-traffic window chosen** — do stage 1 and stage 2 cutovers on weekday morning CZ time (09:00-11:00 CET), NOT Friday evening / weekend. If something breaks, bectly needs to be reachable.

---

## Policy change: Failover → Random (weighted)

P6.1 configured the LB with **Traffic Steering: Off / Failover**. Weighted split requires **Random** steering with weights applied per pool.

Dashboard → Traffic → Load Balancing → edit `janicka-shop.cz` LB:

| Field | Before (P6.1) | After (P6.3) |
|---|---|---|
| Traffic steering | `Off / Failover` | `Random` |
| Default pools | `[pool-hetzner, pool-vercel]` ordered | both listed, weights applied |
| Fallback pool | `pool-vercel` | `pool-vercel` (unchanged) |
| Session affinity | OFF | OFF (still fine — cart is localStorage + DB) |

Weights are set **per-pool-reference inside the LB**, not inside the pool itself. When you switch to Random, CF reveals a "Weight" slider on each pool row. Weights are proportional, not absolute percentages — `hetzner=1 vercel=9` means 10/90 split.

**Important:** fallback pool still kicks in if BOTH weighted pools go unhealthy. Leave it as `pool-vercel` — the health monitor guarantees an unhealthy pool gets 0% regardless of weight.

---

## Stage 1 — Hetzner 10%, Vercel 90% (24h soak)

### Execute

Dashboard → Load Balancing → `janicka-shop.cz` LB → Edit → set:

| Pool | Weight |
|---|---|
| `pool-hetzner` | `1` |
| `pool-vercel` | `9` |

Save. Change takes effect on next DNS resolution (≤LB TTL, typically 30s worldwide via CF edge).

### Verify the split

From two different networks (home + mobile hotspot), over 5 minutes:

```bash
for i in {1..50}; do
  curl -sS https://janicka-shop.cz/api/health \
    | jq -r '.commit // "null"'
  sleep 2
done | sort | uniq -c
```

Expect roughly 5 Hetzner / 45 Vercel (10/90). Small-sample noise is fine; anything outside 3-8 Hetzner suggests routing broke — halt and rollback.

### Monitor for 24h

Check every 4h (or set Telegram alerts — see P7 when it ships):

| Signal | Source | Threshold (abort if exceeded) |
|---|---|---|
| 5xx rate | CF dashboard → Analytics → Status codes, filter last hour | **>2× Vercel baseline** for 2 consecutive 10-min windows |
| p95 TTFB | CF Analytics → Performance | **>1.5× Vercel baseline** sustained 30min |
| Sentry new issues | Sentry → janicka-shop → Issues → "First seen: last 24h" | **any new unique issue** with >5 events — investigate before stage 2 |
| PM2 memory | `ssh kryxon 'pm2 status'` | RSS stable, no restart loop. If `restarts` climbs >3 in 24h, abort. |
| DB query count | Turso dashboard → `janicka-shop` DB → Metrics | should scale ~linearly with Hetzner share (~10% extra queries vs Vercel-only). Spike >30% uplift = N+1 regression, abort. |
| Redis ops/s | `ssh kryxon 'redis-cli info stats \| grep instantaneous'` | ops/s proportional to traffic share. Zero ops/s at 10% load = cache isn't being hit — warn but don't abort. |
| Error logs | `ssh kryxon 'tail -f /var/log/pm2/janicka-shop-error.log'` (spot-check 3× during 24h) | no repeat stack traces, no ECONNREFUSED/ETIMEDOUT to Turso. |

If all thresholds hold for 24h → stage 2.

### Rollback from stage 1

Revert pool weights to `hetzner=0 vercel=1` (or flip steering back to Failover — functionally equivalent when Hetzner weight is 0). Hetzner stays warm, investigate from PM2 logs / Sentry at leisure.

---

## Stage 2 — Hetzner 50%, Vercel 50% (24h soak)

### Execute

Only proceed if stage 1 thresholds all held.

| Pool | Weight |
|---|---|
| `pool-hetzner` | `5` |
| `pool-vercel` | `5` |

### Verify

Same 50-request sampling as stage 1 — expect ~25/25 split.

### Monitor for 24h

Same thresholds as stage 1, **but with stricter absolute numbers** — at 50% share Hetzner is now serving 5× more traffic than stage 1, so any marginal resource issue shows up here.

Extra checks to add at this stage:

- **VPS CPU/RAM** — `ssh kryxon 'top -bn1 | head -20'` should show `%us < 40%` sustained. If CPU pegs, scale PM2 cluster mode instances (add `instances: 2` to `ecosystem.config.js`) OR abort.
- **nginx connection count** — `ssh kryxon 'ss -tan | grep :443 | wc -l'` should stay well under `worker_connections` (check `/etc/nginx/nginx.conf`). Default 1024 is fine for our scale.
- **Turso connection pool** — Prisma's default pool is `num_physical_cpus * 2 + 1`. Hetzner has 4 CPU → pool of 9. Watch for `PrismaClientKnownRequestError: P1001` (timeout). If seen, bump `POOL_SIZE=20` in `.env.production` and `pm2 restart`.

If 24h holds → stage 3.

### Rollback from stage 2

Revert to `hetzner=1 vercel=9` (back to stage 1 weights, not all the way off — stage 1 proved itself). Hetzner continues to serve 10% while you debug. Only fall back further if the issue is traffic-dependent.

---

## Stage 3 — Hetzner 100%, Vercel 0% (failover-only mode, 24h soak)

### Execute

Two equivalent approaches; prefer (A) for cleanness:

**(A) Switch steering back to Failover with Hetzner first.** Restores the P6.1 primary/backup semantics. Vercel gets 0% normal traffic but is instantly available if Hetzner dies.

| Field | Value |
|---|---|
| Traffic steering | `Off / Failover` |
| Default pools (ordered) | 1) `pool-hetzner`  2) `pool-vercel` |
| Fallback pool | `pool-vercel` |

**(B) Keep Random with weights `hetzner=10 vercel=0`.** Identical runtime behavior, slightly more costly (CF still bills as weighted-Random). Use only if we later want gradual shifts back to Vercel for some reason (unlikely).

### Verify

```bash
for i in {1..50}; do curl -sS https://janicka-shop.cz/api/health | jq -r '.commit'; sleep 1; done | sort | uniq -c
# Expect: all 50 responses show Hetzner commit sha.
```

### Monitor for 24h

Same thresholds, plus:

- **Cumulative 72h compare** — pull full 72h window (stages 1-3 combined) from CF Analytics + Sentry:
  - Error rate ≤ 24h Vercel baseline (recorded in prerequisites).
  - p95 latency ≤ 24h Vercel baseline.
  - No unresolved new Sentry issues traced to Hetzner-only requests (check `server_name` tag if Sentry has it; otherwise correlate by commit sha which differs between origins).

If 72h total holds → task #343 acceptance met. Mark Phase 6 P6.3 ✅ in `docs/migration/hetzner-all.md` with the baseline-vs-Hetzner comparison numbers.

### Rollback from stage 3

Flip back to stage 2 weights (`hetzner=5 vercel=5`). This halves Hetzner load immediately while keeping Vercel warm and receiving live traffic (useful for comparison debugging). If stage 3 broke because of traffic volume on Hetzner, stage 2 weights should hold.

Full emergency rollback (Hetzner melting, cannot debug in time): `hetzner=0 vercel=1` — Hetzner gets no traffic, stays warm for debugging, Vercel serves 100%. This is the same state as pre-P6.3.

---

## Baseline snapshot (fill in before stage 1)

Record here at start of P6.3 execution, sourced from 24h of Vercel-only production:

```
Date captured: ____________________
Window: [start UTC] → [end UTC] (24h)

Vercel baseline:
  Requests:                 ______/s peak, ______/s avg
  p50 latency:              ______ ms
  p75 latency:              ______ ms
  p95 latency:              ______ ms
  p99 latency:              ______ ms
  5xx rate:                 ______ % of total
  5xx absolute count:       ______ per 24h
  Sentry new issues:        ______ (last 24h)
  Sentry total events:      ______ (last 24h)
```

These numbers become the **pass/fail gates** for each stage. Do not proceed without them — "eyeballing it" is how regressions ship to production.

---

## Stage log (fill in as you go)

| Stage | Started | Weights (H/V) | Ended | Outcome | Notes |
|---|---|---|---|---|---|
| 1 | | 1/9 (10%) | | ✅ / rollback | |
| 2 | | 5/5 (50%) | | ✅ / rollback | |
| 3 | | 10/0 (100%) | | ✅ / rollback | |

---

## Known gotchas

- **CF edge cache skew between origins:** if any page is cached at the CF edge (set `Cache-Control: public, max-age=X` from origin), users hit the edge, not the origin. During weighted split, two consecutive requests from the same visitor might not route to different origins because the first one filled the cache. Current state: only `/api/health` is `no-store` — most pages are ISR-rendered but served via CF at edge-cache defaults. Impact: real-user latency mostly reflects CF edge, not origin. **Mitigation:** rely on `/api/health` and admin routes (never cached) for routing verification, not on public pages.
- **Session affinity — off, stays off:** the cart is client-side (Zustand → localStorage), reservations are server-side (Prisma). There is no in-memory session state on either origin, so sticky-ness is not needed. If we ever add server-side sessions, revisit.
- **Vercel staying warm:** as long as CF LB probes keep pinging Vercel's `/api/health` every 30s, Vercel should not cold-start. If Vercel scales to zero despite probes, add a scheduled UptimeRobot ping as insurance (P7.1 #340).
- **WebSocket upgrade mid-shift:** admin panel has no WS yet, but `devChat` uses long-polling and webhook inserts. Switching origins between polls is safe because the DB is the source of truth (Turso). No sticky needed.
- **Turso rate limits:** free tier is 500M rows read / 10M rows written per month. At 100% Hetzner, we lose the Vercel → Turso read-replica traffic pattern and gain the Hetzner VPS direct connection. Prisma's query count should stay roughly the same, but watch the Turso dashboard billing page on day 3.
- **DNS propagation myth:** CF LB does not have meaningful DNS propagation — weight changes apply at the CF edge within seconds. The ≤30s TTL is mostly cosmetic. If after 2 min you still see 100% old behavior, check that you saved the LB config, not just the pool config.

---

## Post-acceptance cleanup

Once 72h at 100% passes:

- [ ] Update `docs/migration/hetzner-all.md` Phase 6 — flip P6.3 ✅, add 24/48/72h numbers.
- [ ] Remove or disable the per-stage sampling curls above from any shared documents.
- [ ] Consider disabling `www` hostname LB (if we kept both) to save $5/mo — CNAME `www` → `@` achieves the same redirect via CF zone rules.
- [ ] Open P7.1 (#340 UptimeRobot) if not already in progress — external synthetic probe on `/api/health` via `janicka-shop.cz` (through the LB, not direct-to-origin).
- [ ] Archive Vercel deploys? NO — keep Vercel as warm standby indefinitely. The whole point of P6 is surviving a Hetzner outage. Budget for Vercel Hobby tier is $0 as long as we stay under limits.
